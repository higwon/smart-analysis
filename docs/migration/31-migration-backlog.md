# Migration Backlog

The full, dependency-ordered implementation plan.

> **This backlog is the single Source of Truth for TASK STATUS** (see doc 41 §2). A work spec is
> the source of truth for a task's *scope/contract*, not its status. **Task IDs are stable** —
> never renumber; mark superseded tasks `superseded-by <ID>`.

Priority: P0 (MVP) → P3 (later). Status flow (doc 41 §2c, doc 42): `planned → ready → in-progress →
review → done`, plus `blocked` (orthogonal) and `superseded`. **Status is updated from GitHub
Issue/PR state; never mark `done` before the PR is merged.**

All tasks are `planned` (no GitHub issues/branches/product code exist yet) **except `F00`, which is
`ready`** — its spec exists and it has no predecessor, so it is the startable first task.

Columns: **ID · Task · Category · Depends on · Legacy evidence · Reuse/rewrite · Done-when ·
Prio · MVP · Status**.

## Foundation (F)

| ID | Task | Depends | Legacy evidence | Reuse / rewrite | Done-when | Prio | MVP | Status |
|---|---|---|---|---|---|---|---|---|
| F00 | Repository & Solution bootstrap (Architecture Gate) | — | none | New (minimal skeleton) | 8 projects (ADR-007) + **dependency-inverted reference skeleton (ADR-009/010): App=composition root, Application/UI ⊄ Infrastructure, `Infrastructure → Application` allowed (Port impl), no cycle** + build/Nullable + test project + **minimal arch guard**; **no commercial libs, no domain/algorithm/parser/UI code** | P0 | ✅ | done |
| F01 | Units + Axes + Buffers (checkpoints F01-A/B/C) | F00 | `FW.Data.Quantity`, `RawToRealTransform`, `ImageBaseScanData` | Reuse unit *semantics* (B); rewrite immutable, no statics | A: UnitRegistry+conversions match legacy · B: Axis raw↔real matches legacy · C: `ScanBuffer<T>` ownership (buffer strategy via ADR) | P0 | ✅ | done |
| F02 | DI Composition Root + full Architecture-Test matrix | F00 | none (no DI legacy) | New | App composition root wires Infrastructure adapters → Application/Domain Ports; NetArchTest enforces the **full** doc 11 matrix: Application ⊄ Infrastructure, UI ⊄ Infrastructure, Infrastructure adapters depend only on allowed Application Ports (Infrastructure → Application OK), Domain ⊄ other product assemblies, only App is a composition root. **Parallel with F01/F03** | P0 | ✅ | review (App `CompositionRoot` builds+validates container; smoke resolves ports; NetArchTest type-level matrix + F00 project-ref guard; 278 tests) |
| F03 | Domain dataset model (records) | F01 | `BaseScanData` hierarchy (doc 02) | Rewrite (composition, immutable) | `AfmDataset` + Scan/Profile/Curve/Spectrum records; no WPF | P0 | ✅ | done |
| F04 | Operation contract + registry (explicit DI, ADR-005) | F03, F05 | operation dispatch enums (doc 03) | New (replaces switch, H4) | `IAnalysisOperation`, `OperationDescriptor`, `IOperationRegistry`; module-based explicit registration; duplicate-id check; reference op + tests | P0 | ✅ | review |
| F05 | Provenance record + types | F03 | `ProcessHistoryLog` (doc 06) | Rewrite (structured, serializable) | `Provenance`/`ProvenanceStep` + JSON schema v1. **Parallel with F04** | P0 | ✅ | done |
| ~~F06~~ | ~~Numeric baseline harness~~ | — | — | — | **superseded-by MV00** (baseline extraction is a MigrationValidation task, decoupled from the new domain) | — | — | superseded |

## Domain (D)
| ID | Task | Depends | Legacy | Reuse/rewrite | Done-when | Prio | MVP | Status |
|---|---|---|---|---|---|---|---|---|
| D01 | Channel descriptors + metadata model | F03 | `TiffHeaderModel`, stringly channels | Rewrite (typed) | strong core + extension bag; no `string.Contains` channel logic | P0 | ✅ | done |
| D02 | ROI + MShape domain types | F03 | MShape overlay (doc 05) | Rewrite (domain-free geometry) | ROI types usable by ops + viz. **Not MVP** — MVP flatten uses full-image region (see V02/A01 rationale) | P1 | ✅ | review (clean-room **domain-free** geometry in `SmartAnalysis.Domain.Geometry`: an abstract `Roi` (sub-pixel, pixel-index space) with `Bounds`/`Contains` + a shared **`ToMask(w,h)`** rasterizer (a pixel is inside iff its **centre** is inside the shape **and** within the grid) + `CountInside`; concrete `RectangleRoi` (half-open box so tiles don't overlap) and `EllipseRoi` (inscribed, normalized-radius). Pure, immutable, no WPF; consumers (ROI-aware ops A06/crop/ROI-stats, V06 overlay) follow. **General `OperationInput.Region` wired:** the F04 contract now carries an optional `Roi? Region`; a region-aware op restricts to the region when supplied (whole dataset when null) — **Roughness (A03) is the first consumer** (ISO params over a drawn rect/ellipse ROI, bounds in provenance; harness `regionroughness` gives distinct whole/rect/ellipse Sq). Shell wiring (attach the overlay ROI to any region-capable op) follows. Rasterizer **clips to the grid in double space before the int cast** (a huge coordinate never overflows the Int32 cast), and `RoiBounds` enforces a **fully-finite** box (Left+Width / Top+Height can't overflow to ∞). Render-verified (rect mask = exact w·h; ellipse ≈ π·rx·ry); 10 tests incl. off-grid clamping, **huge far-off-grid ROI still masks the whole grid**, half-open edges, π·r² area, far-edge-overflow rejection) |
| D03 | Force-curve segment + approach/retract domain model | F03 | `SpectroscopyPointData`, PinPoint classifiers (doc 02/03) | Rewrite | segment/approach-retract model for force curves (EPIC-SPEC01) | P2 | – | planned |

## File formats (FF)
| ID | Task | Depends | Legacy | Reuse/rewrite | Done-when | Prio | MVP | Status |
|---|---|---|---|---|---|---|---|---|
| FF01 | TIFF (PSIA) reader → domain | F01, F03, D01 | `TiffReader`/`LIB.File.Tiff` (B) | Extract parser, rewrite mapping | reads scan/profile/spectroscopy TIFF to `AfmDataset`; fixtures | P0 | ✅ | review (MVP = 2D scan image; profile/spectroscopy routed to typed unsupported, follow-up) |
| FF02 | TIFF writer (WPF-free) + provenance | FF01, F05 | `TiffWriter` (D) | Clean-room | writes result TIFF incl. provenance; round-trip | P1 | ✅ | review (new Application port `IScanFileWriter` + `PsiaTiffWriter` (Infrastructure) — hand-emits the LE TIFF container + PSIA tags (magic/data/header) so `PsiaTiffReader` parses it back; pixels written as float32 gain=1/offset=0 (lossless round-trip); X/Y geometry **canonicalized to µm** (the PSIA header's fixed unit — origin affine, step scale-ratio; non-length axes rejected) so a non-µm axis survives. **Identity + X/Y `AxisDirection` + provenance (F05)** embedded as a JSON side-car in the standard `ImageDescription` tag (the header has no faithful scan-direction field, so a Reverse axis from A07 flip/rotate would otherwise round-trip to Forward and shift every physical coordinate), reusing the P01 workspace DTO records so the provenance JSON shape is identical across both carriers. Reader **extended** to restore id+provenance when the side-car is present, else legacy fresh-id + `Root` (real PSIA files unaffected). No WPF, no TIFF lib for writing (ADR-015). DI `AddPsiaTiffWriter()` wired at the root; 5 tests: full pixels/axes/channel round-trip through the real reader, identity+provenance restore (op id/input/params survive), legacy no-side-car → Root, non-image rejected, DI resolves the port) |
| FF03 | PS-PPT reader (Fast PinPoint) | F03 | `LIB.File.PSPPT` (B) | Extract | reads PS-PPT → ForceCurve/ScanImage datasets | P2 | – | planned |
| FF04 | HDF5 reader (PiFM) + preserve provenance | F03, F05 | `LIB.File.HDF5` (A) | Extract (reference design) | reads HDF5; instrument provenance preserved | P2 | – | planned |
| FF05 | Content-based format detection | FF01 | `EOpenFileType` (extension only) | Rewrite | magic-byte sniff + extension fallback | P1 | – | planned |

## Migration validation & testing (MV / T) — baseline **before** analysis implementation
| ID | Task | Depends | Legacy | Reuse/rewrite | Done-when | Prio | MVP | Status |
|---|---|---|---|---|---|---|---|---|
| MV00 | Legacy baseline extraction (golden generation) | legacy repo access only — **NOT** the new domain | `FW.Analysis.Calculate` (UI-free, doc 03) | New (drives legacy engine) | harness/dumps golden JSON (values+units) for chosen ops; records legacy commit/branch, params, input hash, tolerance; normal + edge cases. **Parallel with F00–F05** | P0 | ✅ | review (statistics + 1D/2D poly-fit primitives; flatten orchestration golden deferred) |
| T01 | Fixture + golden corpus (freeze) | MV00, FF01 | samples in `NSISBuild/Sample` (doc 04) | New | fixtures committed/env-gated; golden data frozen with provenance | P0 | ✅ | planned |
| T02 | Per-operation parity test | T01, each A## | — | New | new op output vs golden within tolerance; fails on excess | P1 | – | planned |
| MV01 | Legacy-vs-new comparison report (per op) | T02 | — | New | report of matches / intentional diffs (ADR-backed) | P1 | – | planned |

## Analysis (A) — each is a registered operation (doc 13, ADR-003/005)
| ID | Task | Depends | Legacy (grade) | Reuse/rewrite | Prio | MVP | Status |
|---|---|---|---|---|---|---|---|
| A01 | Flatten (whole/line/surface) op | F04, FF01 | `Whole/Line/SurfaceFlattenProcess` + regressions (A/C) | Reuse numeric, drop WPF Point; **MVP uses full-image region (no ROI)** | P0 | ✅ | review (full-image; fit primitives golden-verified; orchestration golden T02 deferred) |
| A02 | Summary statistics + histogram op | F04 | `SummaryStatisticsCalculator` (A) | Reuse | P0 | ✅ | review |
| A03 | Roughness (ISO 25178) op | F04 | `RoughnessCalculator` (B) | Clean-room (reuse MV00 golden core) | P1 | ✅ | review (`image.roughness` — parameterless areal height parameters **Sa/Sq/Sp/Sv/Sz/Ssk/Sku** relative to the mean plane; numeric core is the MV00-golden `SummaryStatistics` (Sp=max−mean, Sv=mean−min, Sz=peak-to-peak) so parameters are golden by construction; artifact attached to source; 6 tests incl. ISO identity Sz=Sp+Sv + launcher end-to-end. **Surfaces in the launcher + opens the generic form with no shell/UI edits** — the U08 payoff, render-verified) |
| A04 | Spatial filters op | F04 | `ImageFilterProcess`/`ConvolutionFilter` (A/B) | Clean-room | P1 | ✅ | review (`image.filter` — clean-room `SpatialFilters` core: Mean/Median/Gaussian smoothing + Sharpen + Sobel/Laplacian edges, odd kernel size, edge-replicated borders; derived image + provenance; schema = `kind` enum + `size` int. **First real parameterised op driving the U08 generic form** — surfaces under Process, kind→Choice / size→Integer, runs via generic values → Before/After, **no shell code**; render-verified; 13 tests) |
| A05 | Fourier filter / FFT op | F04 | `Image2DFourierFilter` (A) | Clean-room | P1 | ✅ | review (`image.fourier` — clean-room `FourierFilters` core: forward FFT → **ideal radial mask** (low/high/band-pass, band-stop over normalized 0–1 cutoffs, where 1 = the spectrum's **maximum radial frequency** — the diagonal Nyquist corner, radius ÷√2) → inverse FFT. No FFT existed, so an iterative **radix-2 Cooley–Tukey** transform is written here, applied separably; non-power-of-two images are **mean-padded** to the next power of two and cropped back (15×15 fixture → 16×16). Derived image + provenance; schema = `kind` enum + `lowCutoff`/`highCutoff` doubles (schema range [0,1]; ordering check conditional on the band kinds; unused edge canonicalized so an ignored cutoff can't fake history divergence — the A04 lesson). Surfaces under Process, kind→Choice / cutoffs→Number, runs via generic values → Before/After, **no shell code**; render-verified (band-stop strips a periodic scan artefact, keeps topography); 13 tests) |
| A06 | Deglitch op (point/line/region) | F04, D02 | `DeglitchProcess` (C) | Clean-room | P1 | ✅ | review (`image.deglitch` — clean-room `Deglitch` **point despike**: a pixel is a spike when it is non-finite, or deviates from its 3×3 (edge-replicated) median by more than `threshold × the image σ`; a spike is replaced by that local median. Derived image + provenance; schema = single `threshold` (σ multiplier) → surfaces under Process, threshold→Number, no shell code; render-verified (salt-and-pepper spikes removed, smooth bump intact); 8 tests incl. spike replacement, smooth-data untouched, non-finite/dead-pixel repair, threshold sensitivity. Line/region-deglitch (the latter uses a D02 ROI) are follow-ups) |
| A07 | Rotate / Flip (geometry) op (split from old Crop/Rotate/Flip/Pixel/Arithmetic umbrella → A07a crop, A07b pixel/arithmetic) | F04 | `RotateFlip` (A) | Clean-room | P1 | ✅ | review (`image.geometry` — clean-room `ImageGeometry` core: horizontal/vertical flip + 90° CW/CCW + 180° rotation, pure row-major reorder like `SpatialFilters`. Quarter turns swap width/height (`SwapsAxes`). The derived dataset **transforms its scan axes to match the pixel mapping** — each output `Axis` carries the source axis's extent and has its `AxisDirection` reversed exactly when the mapping walks that axis backwards, so `derived.Axis.RawToReal(destIndex)` equals the physical coordinate of the source pixel (pixel + axis stay one consistent world; not a bare reference-swap, which would be reversed). Derived image + provenance; schema = single `kind` enum → surfaces under Process, kind→Choice, no shell code; render-verified (flip/rotate of an asymmetric 96×64 raster, 90° → 64×96); 15 tests incl. algebraic identities (quarter∘quarter=half, cw∘ccw=identity, half=flipH∘flipV) + `RawToReal` axis-equivalence. **A07a crop ✅**: `image.crop` — clean-room `ImageCrop.Extract` (row-major block copy) + a `CropOperation` that represents the region as a D02 `RectangleRoi`, **clamps it to the image**, and rebuilds the cropped scan axes so a cropped pixel keeps its physical coordinate (`derived.Axis.RawToReal(i) == source.RawToReal(offset+i)`, direction-aware — the A07 rule); schema = four int extents → surfaces under Process, runs from the generic form (a drawn ROI can pre-fill them once V06 lands), no shell code; provenance records the **effective (clamped)** crop. **Live crop preview:** while the Crop form is open, the image shows a rectangle overlay mirroring its left/top/width/height (a Crop-form semantic hook in the shell → `AfmImageView.SetRegionPreview`, a screen-space overlay that tracks pan/zoom, clamped to the image, themed `SA.Brush.Chart.Selection`). Render-verified (crop rect outlined on the original + the cropped result; live preview L/D); 8 tests. **A07b pixel/arithmetic ✅**: `image.pixelmath` — clean-room `PixelMath` (Invert = flip about the data mid / AbsoluteValue / Offset / Scale, element-wise; **non-finite pixels copied through before the arithmetic** so NaN/±∞ keep their exact sign/value); derived image + provenance; schema = `kind` enum + `amount` (used only by Offset/Scale, canonicalized to 0 for the fixed transforms so an ignored amount can't fake history — the A04 lesson); provenance records the amount in the **channel unit for Offset** (it adds a physical value) and dimensionless for Scale (a factor); fifth op on the U08 generic form (kind→Choice, amount→Number), no shell code; render-verified (Invert/Scale/Offset over a fixed range); 14 tests) |
| A08 | PSD / power-spectrum op | F04, V03 | `LinePowerSpectrum`/`PSDStatistics` (A/B) | Clean-room | P2 | ✅ | review (`image.psd` — clean-room **1D line-average PSD**: the periodogram of each fast-scan line (along X), averaged over lines. Each line is mean-subtracted, zero-padded to the next power of two, and transformed with a **shared radix-2 FFT** (`FastFourierTransform`, **extracted from A05** `FourierFilters` so there is one FFT primitive — A05's 13 tests are the safety net); the one-sided PSD is `C_k = (dx/N)·|Z_k|²` with interior bins doubled, **Parseval-normalized** so `Σ C_k·Δf = line variance` (magnitudes physical, independent of padding). **First curve-producing op**: outputs a `LineProfileDataset` (not an image/scalar) — X = spatial frequency (reciprocal of the image's X length unit, a real `WaveNumber` unit `1/um`), value = PSD in `[Z]²·[X-len]` — a **real composite unit**: dimension `{Zdim}^2*{Xdim}` and `ScaleToBase = ScaleZ²·ScaleX` (so `nm²·um` ↔ `um²·um` convert but `nm²·um` ↔ `pN²·um` do not, and the base scale is physical, not a display-only `1.0`); the op **validates the X axis is a length** (the spatial PSD's reciprocal-length axis is undefined otherwise). Uniform frequency axis (DC dropped, origin = step = Δf). Parameterless → surfaces under Process, generic form (no fields), **no shell code**. **V03 curve view wired into the shell** (the payoff): the stage now routes the active dataset by type — an image → `AfmImageView` (`IsSingleImage`), a 1D profile → `AfmCurveView` (`IsSingleCurve`), mutually exclusive (`ShellViewModel.ActiveCurve`; `RenderInputFactory.ForLineProfile` → the V03 control). Render-verified headlessly (a bin-aligned tone → one sharp spectral peak at exactly `cycles/(N·dx)`, axes `Frequency (1/um)` / `PSD (nm²·um)`); harness `psd` (cheese real-data + synthetic tone, peak bin exact). 16 tests: 6 core (peak location, Parseval energy = A²/2, flat→zero, non-finite line skip), 7 op/launcher (curve output, reciprocal-length + composite value units w/ correct base scale, non-length X rejection, provenance, Process payoff), 3 shell curve-routing. Radial/2D PSD, windowing (Hann/Welch), and log-log display are follow-ups) |
| A36 | Line profile / cross-section op | F04, V03 | profile cursor (doc 05) | Clean-room | P2 | ✅ | review (`image.profile` — clean-room `CrossSection`: extracts a single **row** (along X) or **column** (along Y) of the image as a spatial 1D `LineProfileDataset` (Z vs position), grid-aligned (no interpolation, values copied exactly). The **second curve-producing op** and the first yielding a **spatial** curve (position in the source length unit, complementing A08's frequency curve) — routes to the V03 curve view wired in A08. The profile **reuses the source scan axis** (X for a row, Y for a column) so each sample keeps its physical coordinate incl. a **reversed** axis direction (`profile.X.RawToReal(i) == source.Axis.RawToReal(i)`, the A07 rule — no re-derivation); channel/unit are the image's. Schema = `orientation` enum + `index` int → surfaces under Process, generic form (orientation→Choice, index→Integer), **no shell code** (a drawn cut can pre-fill them later). Render-verified (harness `profile`: cheese mid-row → 15-pt spatial profile, axes `X (um)` / `Topography (um)`); 10 tests: 4 core (row/column slice, out-of-range, empty), 6 op/launcher (row-along-X + column-along-Y direction-aware axis reuse, RawToReal equivalence, index-per-orientation rejection, provenance, curve-output Process payoff). Arbitrary-angle line profiles (interpolated) + a draggable cut overlay are follow-ups) |
| A37 | Arbitrary-angle line profile op | F04, V03, A36 | profile line cursor (doc 05) | Clean-room | P2 | ✅ | review (`image.line-profile` — clean-room `LineSampler`: samples the image along the segment between two **endpoints** (pixel coords) by **bilinear interpolation** — the drawn-line profile (a line at any angle, the legacy interaction), complementing A36's exact grid-cut. Produces a spatial 1D `LineProfileDataset` of Z vs **arc length**: the endpoint pixel deltas are converted through the X/Y axis steps to a physical distance (so a diagonal is measured correctly, and correctly even when X and Y units differ — computed in the base unit, expressed in the X length unit); a point whose bilinear neighbourhood is non-finite yields NaN (dropout not interpolated away). Validates length X/Y axes, in-bounds endpoints, non-zero length. Schema = four endpoint coords + a sample count → surfaces under Process, generic form (endpoints→Number, samples→Integer), **no shell code** — **a drawn line overlay will pre-fill the endpoints** (the interactive draw-on-image + live chart is the immediate follow-up; this is its data layer). Render-verified (harness `lineprofile`: cheese diagonal → 128-pt curve, `Distance (um)` axis, arc-length 1.573 µm = √2 × the 1.113 µm row extent); 11 tests: 5 core (horizontal exact, sub-pixel interpolation, diagonal, NaN neighbourhood, <2 samples), 6 op/launcher (arc-length = X extent, 3-4-5 diagonal = 20 µm, cross-unit µm/nm arc length, zero-length + out-of-bounds rejection, provenance endpoints, curve-output Process payoff)) |
| A09 | Grain / particle op | F04 | `GrainDetector`+`SequentialLabeler` (A/B/C) | Clean-room | P2 | ✅ | review (`image.grains` — clean-room `GrainDetector`: 8-connected components (iterative flood fill, no recursion limit) of pixels ≥ a normalized height threshold, dropping regions below a min area. **Measurement** artifact (like A03): GrainCount / Coverage (over the **finite** sample area) / MeanArea (px) / MeanHeight (Z unit); threshold given as normalized 0–1 mapped into the image min…max (scale-free, reproducible); non-finite pixels excluded from grains **and** the coverage denominator + warned. Fourth op on the U08 generic form — surfaces under **Measure**, threshold→Number / minArea→Integer, no shell code; render-verified (5 tall bumps detected, 2 faint rejected); 10 tests incl. 8-connectivity, min-area speck rejection, coverage/mean-height. Physical-area unit (Unit²) a follow-up) |
| A35 | Region statistics (ROI Sa/Sq/Sz) op — **rect + ellipse** | F04, D02, A03 | region-of-interest measurement | Clean-room (reuse MV00 golden core over a D02 mask) | P1 | ✅ | review (**Ellipse ROI added:** a `shape` enum (Rectangle/Ellipse) selects a D02 `RectangleRoi` or **`EllipseRoi`** — the first consumer of `EllipseRoi`; the ellipse selects ≈ π/4 of the bounding box (verified) and the shape is recorded in provenance (so an ellipse and rect run differ in history). **The ROI is built on the effective (clamped) bounding box, not the raw request** — for a rectangle clamping equals clipping, but an overhang *ellipse* built on the raw box would be a larger, off-centre ellipse whose clipped half differs from the inscribed ellipse of the clamped box (the displayed ROI); so `Ellipse(0,0,100,100)` and `Ellipse(0,0,20,20)` on a 20×20 image give identical mask/stats/provenance (verified), keeping displayed ROI = measured mask = provenance. The V06 overlay draws the **inscribed ellipse** (accent fill) inside a **dashed bounding box** with the same 8 resize handles when the form's `shape` = Ellipse (`AfmImageView.RegionIsEllipse`, wired from the shell's region form); render-verified L/D (a centred ellipse ROI on the cheese scan, Shape=Ellipse in the form); +1 op test (π/4 + shape provenance) + 1 control test (ellipse shown + dashed bounding rect). Original: `image.roi-statistics` — height statistics + roughness (**Sq/Sa/Sz/Mean/Min/Max/PixelCount**) over a **rectangular ROI**; the first consumer of the D02 `RectangleRoi.ToMask(w,h)` — a pixel counts iff its centre is inside the region **and** finite. Numeric core is the same MV00-golden `SummaryStatistics` as A03, so a full-image ROI is **identical to A03 whole-image roughness** by construction **when every pixel is finite** (harness `roistats`: full=overhang(clamped)=A03; a centre sub-region selects fewer pixels) — they diverge intentionally on non-finite images, since A35 **excludes** non-finite pixels while A03 currently includes them (yielding a non-finite result). **Measurement** artifact attached to source (like A03/A09); non-finite pixels excluded + warned, empty region warned (independent warnings, so an all-non-finite region raises both); **provenance is canonicalized to the effective (clamped) extent** so two requests selecting the same pixels — e.g. `(0,0,100,100)` and `(0,0,4,4)` on a 4×4 — share one history (the A04/crop lesson); height scalars carry the channel unit, count is dimensionless. Schema = four int extents → surfaces under **Measure**, all→Integer, no shell code. **Shell region overlay generalized:** the draggable V06 region overlay (previously Crop-only) now activates for **any** form exposing left/top/width/height (`HasRegionFields`), so ROI Statistics gets the same drag-to-select rectangle two-way-bound to its form. 7 tests (golden parity over the masked pixels, grid clamping, non-finite exclusion, units, origin-outside rejection, launcher Measure payoff). Ellipse/polygon ROIs + a general `OperationInput.Region` for ROI-aware ops are follow-ups) |
| A10 | Profile **filter** op (split → A18, A19) | F04, FF01 | `ProfileFilter` (A/B) | Reuse | P2 | – | planned |
| A11 | Spectroscopy **filter** op (split → A20, A21, A22) | F04 | `SpectroscopyFilter` (A core) | Extract numeric | P2 | – | planned |
| A12 | Modulus (FD + Oliver-Pharr) op | F04 | `ModulusCalculator`+`NRFitter` (C/A) | Extract numeric | P2 | – | planned |
| A13 | FD measures op (split → A23) | F04 | `FDSpectroscopyCalculator` (C/A) | Reuse | P2 | – | planned |
| A14 | Spectrum **matching + ranking** op (split → A32, A34) | F04, A32, P02 | matchers (A) | Reuse | P2 | – | planned |
| A15 | **Peak detection** op (split → A31) | F04 | `PeakDetector` (A) | Reuse | P2 | – | planned |
| A16 | Stitch (managed blend/preview) op | F04 | `StitchBlend/Preview` (A/B) | Reuse | P3 | – | planned |
| A17 | Stitch (native engine) op — ADR first | A16 | `LIB.External.Stitch`+native dll (C) | Wrap or reimplement (ADR) | P3 | – | planned |
| A18 | Profile flatten op | F04, FF01 | `ProfileFlattenProcess` (A/B) | Reuse (share flatten core) | P2 | – | planned |
| A19 | Profile crop op | F04 | `ProfileProcessCrop` (D) | Rewrite (cursor→ROI) | P2 | – | planned |
| A20 | Spectroscopy slope-adjust op | F04 | `SpectroscopySlopeRegression` (A core) | Reuse | P2 | – | planned |
| A21 | Spectroscopy offset-adjust op | F04 | `OffsetAdjust` (D) | Extract numeric | P2 | – | planned |
| A22 | Force constant / sensitivity op | F04 | `ForceConstant` (D) | Extract numeric | P2 | – | planned |
| A23 | Approach/Retract split op | F04, D03 | PinPoint classifiers (A) | Reuse | P2 | – | planned |
| A28 | PiFM smoothing op | F04 | `SmoothingFilter` (A) | Reuse | P2 | – | planned |
| A29 | PiFM baseline correction op (linear + ALS) | F04 | `BaselineCorrection` (A core) | Reuse | P2 | – | planned |
| A31 | Spectral range statistics op | F04 | `SpectralRangeAnalyzer` (B) | Reuse; **fix FWHM TODO** (doc 07 M5) | P2 | – | planned |
| A32 | Spectrum preprocessing ops | F04 | 7 preprocessors (A) | Reuse | P2 | – | planned |
| A34 | Spectrum difference/overlay op | F04 | overlap/difference (doc 03) | Reuse/rewrite | P2 | – | planned |

## Workspace / Persistence (W / P)
| ID | Task | Depends | Legacy | Reuse/rewrite | Prio | MVP | Status |
|---|---|---|---|---|---|---|---|
| W01 | Workspace model + active-context | F03, F05 | tray/navigator (fused, doc 02/05) | Rewrite | P0 | ✅ | review |
| P01 | Workspace file save/reopen w/ lineage | W01, F05, FF01 | **none** (doc 06) | New | P0 | ✅ | review (directory-package v1, buffers inline; relink-by-reference + re-run deferred) |
| P01-UI | Workspace Save/Open in the shell | P01, U01 | DevExpress dialogs | New | P0 | ✅ | review (`IWorkspacePersistence` use case over the `IWorkspaceStore` port + a WPF folder picker; **Open** button + activated **Save** in the command bar; Open adopts the restored workspace **in place** via new `Workspace.ReplaceWith` (moves datasets + active + lineage; keeps every view-model bound); silent re-save to the known folder; unsaved-dot tracking; typed open-failure surfaced; render-verified; 3 tests: ReplaceWith move/lineage, save→open round-trip, non-workspace failure) |
| P02 | Spectrum library (SQLite) relocate | F03 | `LIB.File.SQLite` (B) | Reuse, fix layering | P2 | – | planned |
| P03 | Schema versioning + migration | P01 | HDF5 strict validator (no migration) | New | P2 | – | planned |

## UX & Design System (UX / UIX) — design tasks (UIX01/UIX02 no code; UIX03 implements resources)
| ID | Task | Depends | Legacy | Output | Prio | MVP | Status |
|---|---|---|---|---|---|---|---|
| UX01 | Core AFM workflow & Information Architecture | doc 17 principles; stable F03/W01 concepts | UI analysis (doc 05) | IA + journeys + active-context meaning + wireframes/text structure + keep/merge/remove + dialog criteria + MVP screen flow. **Parallel with V00.** No code | P0 | ✅ | ✅ done (doc 22; PR #33 merged, user-approved) |
| UIX01 | First-party Design System foundation (no code) | UX01, doc 21, ADR-008 | DevExpress theme (doc 05) — reference only | tokens (palette/semantic/typography/spacing/size/radius/border/focus/status/chart-image/density), theme-swap principle, simple-modern rules, forbidden patterns, design-system doc | P0 | ✅ | ✅ done (doc 23; PR #35 merged, user-approved incl. AA Error-banner fix) |
| UIX02 | MVP visual design + high-fidelity Light/Dark screens (no code) | UIX01 | — | approved Light+Dark visuals for the MVP screens (shell/explorer/viewer/flatten panel/before-after/history/progress/empty/loading/error/save). **User approval is a required gate before U01/U02** | P0 | ✅ | review (doc 24 + hi-fi artifact, all screens L/D; ★awaiting user approval — gate before UIX03/U01/U02) |
| UIX03 | WPF tokens, styles & component mapping (**implements resources**) | UIX02 (approved) | — | ResourceDictionary structure, token keys, base/variant/component styles, Light/Dark swap, VisualStates, external-control styling adapter, no-hardcoded-values rule, style validation | P0 | ✅ | review (SmartAnalysis.UI/DesignSystem: tokens+L/D palettes+shared button template+ThemeManager; SA.* keys; 5 style-validation tests incl. screen-metric lint; builds+271 tests+STA swap check) |
| UIX04 | Iconography — first-party vector icons (**implements resources**) | UIX03 | DevExpress icons (doc 05) — not reused | Lucide (ISC) vendored as `SA.Icon.*` WPF geometries + `IconPresenter` (currentColor, theme-swap); doc 25 policy + ADR-019 + doc 20 Approved; a11y icon+text | P0 | ✅ | review (22 MVP icons; converter tool; 7 style tests; builds; L/D render sheet verified) |
| UIX05 | Design-system control finishing + theme verification | UIX03 | — | Thin themed **`SA.ScrollBar`** (rounded thumb, no arrows, hover/drag tones) as the implicit default — replaces the grey Windows scrollbar in the Navigator tree / Inspector / provenance list / multi-line text (esp. Dark); reuses existing neutral brushes (no new palette keys). New **`SmartAnalysis.UiTests`** (net8.0-windows) unblocks WPF-dependent tests: `ThemeManager` state/resolve + **live palette-swap** + `ThemePreferenceStore` round-trip/guard. **Fixes the real in-app Light/Dark toggle** — the palette was being replaced in the *nested* `DesignSystem.xaml` dictionary, which updates lookups but does not invalidate live `DynamicResource` consumers (button flipped `EffectiveTheme` without repainting); now swapped at the **top level** of `Application.Resources.MergedDictionaries` (the mechanism WPF propagates to every window). **Fully re-templates `ComboBox`** (a setter-only style left the default WPF chrome painting system-grey → unreadable off-theme, esp. Dark) + a guard test that every interactive control ships a `ControlTemplate`. **Responsive viewer toolbars** — the single-image + compare toolbars used overlapping `HorizontalAlignment` panels in one cell (collided at the 1000px min window); rebuilt as `Auto/*/Auto` columns with the low-priority info clipped, so nothing overlaps at any size | P1 | ✅ | review (ComboBox + ScrollBar + toolbars L/D render-verified at min/wide; live toggle repro'd + fixed + regression-locked; 10 UiTests + 6 control-template guards; arch-guard → 9 projects; 318 tests green) |
| UIX06 | Template the remaining setter-only controls | UIX05 | — | Full `ControlTemplate`s (SA brushes, L/D) for the controls left setter-only after the ComboBox fix — **RadioButton** (accent indicator), **Slider** (rail + accent thumb), **Expander** (rotating chevron header), **Separator**, **TabControl**, **Menu / MenuItem** (all four roles: submenu item/header + top-level item/header, with themed pop-ups) / **ContextMenu**, and **DataGrid** (control template + `DataGridColumnHeader`/`Cell`/`Row` parts + themed scrollbars). No control now falls back to system-coloured WPF chrome; the control-template guard extended to cover them. **Preserves WPF built-in semantics** (not just chrome): Slider Horizontal/Vertical, TabControl Top/Bottom/Left/Right `TabStripPlacement`, Expander Down/Up/Left/Right `ExpandDirection`, and a checkable MenuItem's `IsChecked` checkmark | P2 | ✅ | review (control gallery render-verified in L/D incl. vertical slider / bottom tabs / up expander; 15 control-template guards + 4 semantics UiTests on a shared WPF host; 331 tests green) |
| UIX07 | DataGrid advanced-feature verification (owning its template) | UIX06 | — | Real-usage check of the built-ins the first DataGrid template dropped: restored the **sort-direction glyph** + **column-resize grippers** on the header, the **select-all corner** button + **row-header** column layout in the control template, a **`DataGridRowHeader`** style, a **cell validation** visual (`Validation.HasError`), and **frozen-column**-aware alignment (`CellsPanelHorizontalOffset` / `NonFrozenColumnsViewportHorizontalOffset`) | P2 | ✅ | review (feature-rich grid render-verified L/D — sort glyph, row+column headers, select-all corner, frozen column; 9 DataGrid-feature guard cases; 340 tests green; cell validation is edit-time per WPF) |
| UX02 | Product Interaction Architecture + Visual Product Design (**no code**) | U01/U02 (slice proven), UIX01–04, doc 22 | legacy full workflow/density (doc 05) — analyzed, not cloned | Stage-first shell; surface-depth; **operation launcher**; role-switching **Inspector**; **comparison mode**; command taxonomy + feature-placement matrix; hi-fi L/D artifact. **★ approval gate before finalizing U02 visual** | P0 | ✅ | review (doc 26 + hi-fi artifact; contracts-to-amend doc 22/24; ★awaiting user approval) |

## Visualization (V)
| ID | Task | Depends | Legacy | Reuse/rewrite | Prio | MVP | Status |
|---|---|---|---|---|---|---|---|
| V00 | Rendering spike + lib decision (ADR) | F03 | SciChart usage (doc 05) | New (Candidate libs) | P0 | ✅ | review (ADR-018: ScottPlot 5 → Approved; spike compares ScottPlot vs OxyPlot on 1M pts + real swap-test behind ICurveView; live WPF interaction deferred to V03/U02) |
| V01 | Viz adapter interfaces + render inputs | F03 | conversion seam (doc 05) | New | P0 | ✅ | review (render inputs + view ports + Domain→render converters + domain Colormap; lib-agnostic) |
| V02 | **Basic** 2D image view (render + palette + zoom/pan, **no ROI**) | V00, V01 | WPF image path (survives) | Reuse approach | P0 | ✅ | review (Visualization `ImagePixelMapper` (pure, tested) + UI `AfmImageView` WriteableBitmap/zoom-pan/legend impl of `IImageView`; real cheese scan rendered; no ROI. **Interaction refined to legacy-style (U02+):** fit is now the **zoom-out limit** (image fits & stays centred, not freely draggable); the wheel **zooms toward the cursor**; pan is enabled **only when zoomed in past fit** and clamped so no edge enters the viewport; math extracted to the pure, testable `ImageViewportMath` (10 UiTests) + control smoke. **Large-scan render fix:** a scan bigger than the viewport (e.g. a 4096² 4k wafer) rendered **tiny** — the `Image` (`Stretch=None`) was arranged at the clamped viewport size, so WPF realized it at viewport resolution and the zoom transform shrank it further; hosting the `Image` on a `Canvas` (unconstrained measure) arranges it at natural pixel size so the transform scales full-res content, and it fits correctly (regression test: Image arranges at natural size). **Palette rework (legacy-parity):** a clean-room `ColormapCatalog` reimplements the legacy procedural palettes (Gold/Grayscale/Aqua/Brown/Melon/Red/Green — 256-entry LUTs by the legacy formulas) replacing the 2-way toggle; the toolbar now has a **colormap picker** + an **Auto/Manual value range** ("palette range specification"): Auto = each image's data min/max, Manual = user min/max threaded through `RenderInputFactory.ForImage`'s range. **Interactive palette bar (legacy-style):** the legend is now a `PaletteBar` whose axis is the fixed data extent with **draggable min/max handles** that set the value window (drag geometry in the pure `PaletteBarMath`; live bar feedback during drag, image re-render on commit); handles are read-only on the Before/After panes. Compact toolbar (no Export overflow at min width); L/D render-verified; 16 tests. Complex table/enhancer legacy maps (Contrast/Terrain/Seismic/Rose) + percentile-range mode are follow-ups) |
| V03 | XY curve view (chart lib behind adapter) | V00, V01 | SciChart charts | Rewrite | P1 | ✅ | review (UI `AfmCurveView` impl of V01 `ICurveView` on **ScottPlot 5** (ADR-018) — hosts an interactive `WpfPlot`; plot composition in a testable `CurvePlotBuilder` (ScottPlot-core, headlessly renderable); themed from the SA `Chart.*` tokens (L/D); borrowed series copied, retains nothing (ADR-011). Real curve rendered L/D via the same builder; 5 tests (plottable-per-series, axis titles, reconfigure-clears, control smoke). **Shell wiring done with A08 PSD** (the first curve-producing op): the stage routes the active dataset by type — a 1D profile → `AfmCurveView` (`IsSingleCurve`), an image → `AfmImageView` (`IsSingleImage`), mutually exclusive; 3 curve-routing tests) |
| V04 | 3D surface view (HelixToolkit) | V00, V01 | SciChart3D/Helix | Rewrite (first-party WPF 3D) | P2 | ✅ | review (a **first-party** 3D height-field surface on a plain WPF `Viewport3D` — **no HelixToolkit / SciChart3D** (ADR-018 rationale extends to 3D). New V01 port **`ISurfaceView`** consuming the **same `ImageRenderInput` as the 2D view** (Z + range + colormap + axes; no new DTO). The mesh is the pure, tested **`SurfaceMeshBuilder`** (Visualization, WPF-free): a row-major scan → positions on a (decimated) grid + smooth per-vertex normals (averaged, area-weighted face normals) + a per-vertex colormap texture coordinate (normalized height); the footprint is **aspect-preserving from the PHYSICAL X/Y extent** (not the pixel count) — a 10&#160;µm × 2&#160;µm scan renders 5:1 even on a square pixel grid; `AxisView` now carries `ScaleToBase` so extents in **different units** (1&#160;µm × 500&#160;nm) compare correctly (span = `|End−Start|·ScaleToBase`), the control passing the base-unit spans to the builder. Height is a centred/exaggerated band. **Large scans are decimated** to a resolution cap with **endpoint-inclusive** mapping (`sx = round(g·(width−1)/(gw−1))`), so index 0 **and** the far edge (width−1) are always sampled and the mesh fills the full extent (a plain stride would drop the far row/column); a 4k scan → a 256-grid (not 16M verts); non-finite samples sink to the range floor. `AfmSurfaceView` (UI) converts the mesh to a `MeshGeometry3D`, colours height with a **256×1 colormap LUT texture** (the same palette as the 2D view) under a directional + ambient light, and orbits (drag) / zooms (wheel). Render-verified (a Gaussian bump + ripples → a lit, gold-mapped surface with correct peak highlights and valley shading); 9 builder tests (grid/index counts, decimation to the cap, **endpoint-inclusive far-edge decimation**, flat→up-normals + constant height, texcoords span the range, **physical (not pixel) aspect footprint** + pixel fallback, NaN→floor, degenerate→empty) + a control smoke (mesh built, cleared). **Shell 3D toggle wired:** a **"3D" toggle** in the single-image toolbar swaps the 2D view for the surface (`ShellViewModel.Is3D` → `ShowSingle2D`/`ShowSingle3D`, mutually exclusive; the surface renders the **same `ImageRenderInput`** as the 2D view with the current colormap/range); the mode **persists across active-image changes**, is ignored for Before/After + curves, and Export captures whichever view is **actually showing** (routed on `ShowSingle3D`, not raw `Is3D`, so an editor forcing 2D exports the visible 2D image — `ChooseExportTarget`, unit-tested). **Interactive overlay editors force 2D:** because the V06/V07 region + line overlays live on the 2D view, opening an overlay editor (`IsInteractiveImageEditing`, detected by the region/line parameter shape) **forces the 2D stage even when 3D is preferred** and hides the 3D toggle, so the surface never covers the overlay the user is dragging; closing the editor restores the retained 3D preference. The stage mode is set **before** the editor change so the 2D image re-renders before the shell seeds the overlay onto it, and `RenderImages` routes on `ShowSingle3D` (not raw `Is3D`) so the editor case renders the 2D view. Render-verified L/D (the cheese scan as a lit gold 3D surface in the shell with the toggle checked; and 3D-preference-on + line-profile open → the 2D image + line overlay + live chart, toggle hidden); +5 shell tests (toggle swaps + re-renders, mode persists, a curve is never a surface, a line-profile editor forces 2D + restores 3D on close, a region editor forces 2D)) |
| V05 | Export (image/3D/CSV/JCAMP) OSS | V02, V03 | export (E) | Rewrite | P2 | – | planned |
| V06 | ROI overlay + interaction | D02, V02 | MShape overlay (doc 05) | Rewrite | P1 | ✅ | review (the Crop region overlay is now **draggable**: drag the body to move, or one of **eight edge/corner handles** to resize — clamped to the image with a 1px minimum, and written **two-way** back into the Crop form's left/top/width/height. Hit-testing + move/resize math is the pure, tested `RegionEditMath` (screen↔pixel via the image transform, edge/corner classification, per-handle clamped resize); `AfmImageView` hosts the handles in the screen-space overlay layer (constant stroke, tracks pan/zoom), themed accent. The displayed box, the drag source, `RegionChanged`, and the effective crop are **one clamped `_effectiveRegion`** (`RegionEditMath.ClampToImage`), so an over-large form value doesn't drag from its raw value. Render-verified (8 handles on the region, L/D); 18 math + 2 control tests. Rectangle only for now — ellipse/polygon overlays follow the shapes) |
| V07 | Line-profile overlay + interaction | V02, A37 | profile line cursor (doc 05) | Rewrite | P1 | ✅ | review (the legacy **draw-a-line-on-the-image** interaction: opening the free line-profile form (A37 `image.line-profile`) draws a **draggable 2-point line** over the image — drag either **endpoint** (round handles) or the **body** to move the whole line, each endpoint clamped into the image, written **two-way** back into the form's x0/y0/x1/y1 (Apply then materializes the profile curve). Drag math is the pure, tested `LineEditMath` (endpoint moves alone; body moves both rigidly — the largest shift that keeps both endpoints in-bounds, so the line never deforms at an edge); `AfmImageView` hosts a `Line` + two endpoint handles in the same screen-space overlay layer as V06 (constant stroke, tracks pan/zoom, themed accent), with the shown/dragged line the single clamped `_effectiveLine`. **One effective line end-to-end (the V06 lesson):** the displayed line, the drag source, the `LineChanged` event, the form's x0/y0/x1/y1, the executed line, and the provenance are all the same clamped line — the control clamps the display/drag to `_effectiveLine`, the shell canonicalizes the form back to it, and the `image.line-profile` op **clamps the endpoints (never rejects) and records the effective ones**, so an overhang request `(−10,7)→(30,7)` on a 15×15 image is the line `(0,7)→(14,7)` everywhere. The shell generalizes the crop-style wiring to any form with **x0/y0/x1/y1** fields and **seeds a default mid-line** across the active image so there is a line to drag from the start. Region and line overlays are mutually exclusive (a region form clears the line and vice-versa). Render-verified L/D (a horizontal line + endpoints over the cheese scan, the op form in the Inspector); 6 math + 2 control + 2 op-canonicalization tests. Arbitrary free-hand draw-from-scratch + a **live profile chart** (split view, updating during the drag) are the immediate follow-ups) |
| V08 | Live line-profile chart (split view) | V07, A37, V03 | profile chart panel (doc 05) | Rewrite | P1 | ✅ | review (completes the legacy draw-a-line-and-see-its-profile loop: while the line-profile form is open the stage **splits** — the image (with the V07 draggable line) on top, a **live profile chart docked below** that redraws as the line is dragged, **without committing to the workspace** (Apply still materializes the profile dataset). The preview is a new Application use case **`ILineProfilePreview`** (`Application → Analysis`, clean layering) computed over a shared **`LineProfileBuilder`** — **extracted so the op and the preview sample the identical effective line, arc-length axis, and clamping** — and projected to a `CurveRenderInput` from a **transient** dataset (never added to the workspace, buffer disposed after projection; no provenance). **Lifetime (ADR-011) verified:** `ForLineProfile` copies the values into owned `double[]` series arrays + a scalar `AxisView`, retaining no reference to the dataset buffer, so the returned input is fully self-contained and the transient dataset is safely disposed before the method returns — a regression test reads the input's values **after** the dataset has been disposed (with pool-churn) and they are still correct. Returns null (chart clears) for a degenerate or non-spatial line. The shell renders the preview into a docked `AfmCurveView` on every endpoint/body drag and form edit, using the **same effective line** the overlay shows (V07). Render-verified L/D (the cheese mid-line drawn on the image + its height profile in the docked chart, axes `Distance (um)` / `Topography (um)`, the op form + Apply in the Inspector); 5 preview-use-case tests (valid curve input, **owned input valid after the transient dataset is disposed**, effective-clamped line, degenerate → null, non-spatial → null) + the shared builder covered by the A37 op tests. Live drag-throttling + a movable split are follow-ups) |

## UI (U)
| ID | Task | Depends | Legacy | Reuse/rewrite | Prio | MVP | Status |
|---|---|---|---|---|---|---|---|
| U01 | Shell (five-region; first-party styled) + workspace explorer | F02, W01, UX01, **UIX03 (visual design approved, ADR-008)** | DevExpress shell | Rewrite | P0 | ✅ | review (MainWindow + ShellViewModel over the real Workspace; SA.*/SA.Icon.* only; Import/sample via IScanFileReader; theme toggle; L/D render verified. **Fixed-Grid layout — AvalonDock docking deferred to a follow-up + its ADR**; lightweight in-house MVVM) |
| U02 | Image analysis page + flatten panel (before/after) | U01, V02, A01 | ImageAnalysis + ImageProcess | Rewrite | P0 | ✅ | review (AfmImageView in Active View via render-orchestration; contextual 4-param Flatten panel → `IImageAnalysisUseCase` (Application, so UI ⊄ Analysis) → derived active + Before/After split; use-case unit-tested; L render verified) |
| U03 | Product shell rework to UX02 (Stage-first shell + Inspector role model + operation-launcher **surface**) | U01, U02, F04, **UX02 (doc 26, approved)** | process dialogs | Rewrite | P1 | ✅ | review (MainWindow recomposed Stage-first per doc 26: recessed Navigator/Inspector rails + elevated Stage; **Analyze ▾** launcher — **hardcoded MVP items (Flatten/Statistics)**, not yet registry-driven → see U08; 4-role Inspector (Dataset/Operation/Result/Step); semantic Flatten editor (segmented Scope/Orientation, Order stepper); `image.statistics` runs → **measurement preserved as an attached `AnalysisArtifact`** in the new `MeasurementStore` (beside the dataset-only Workspace), surfaced as an attached explorer node + re-selectable Result card (active unchanged); rail-docked provenance strip (step = read-only, not navigable); Before/After independent-Z compare; backend (use case/operation/viewer) unchanged; directional-hairline tokens added; L/D render verified) |
| U04 | **Profile analysis UI** (split from old curve/spectrum umbrella → U06, U07) | U01, V03, A10 | Profile pages | Rewrite | P2 | – | planned (V03 follow-up: on shell wiring, re-`Render` / add `AfmCurveView.Retheme()` on `ThemeManager.ThemeChanged` — the curve backend resolves the theme at Render time only, so a live L↔D swap needs an explicit re-compose) |
| U05 | Provenance/history panel | U01, F05 | (none visible) | New | P1 | ✅ | review (the History/Provenance panel now surfaces each recorded step's **real, auditable detail** from F05, not just the op name: selecting a step shows the **parameters that were applied** (name + value **with unit**, dimensionless values dropping the `1`) and any **warnings** recorded when it ran (e.g. clamped region, skipped non-finite lines), plus the raw operation id. The Inspector value is the **exact recorded value** (shortest round-trippable form — what is shown equals what ran, so `0.123456789` is not truncated), while the compact strip **summary** may round (`G4`) for a glance. `HistoryRowViewModel` carries a structured `Parameters` (a `StepParameterViewModel` list) + `Warnings` + a compact `Summary` (built from `ProvenanceStep.Parameters`/`Warnings`); the strip column shows the summary, the Step-role Inspector renders the parameter table + a themed warnings block; a step stays **read-only + non-navigable** (selecting it never changes the active dataset). The Import (root) row shows the source file and no parameters. Render-verified L/D (Fourier step → `image.fourier`, kind/lowCutoff/highCutoff table in the Inspector + the strip summary); 5 ShellViewModel tests (params+units+warnings exposed, lossless Inspector value vs rounded summary, physical-unit symbol kept, import row, read-only selection). Enum parameters currently show their numeric code (a friendly enum-name needs the op schema, not in the provenance record) — a follow-up) |
| U06 | Spectroscopy analysis UI | U01, V03, A11 | Spectroscopy pages | Rewrite | P2 | – | planned (V03 follow-up: confirm `SignalXY`'s monotonic-X assumption holds for spectroscopy; if X is non-monotonic / irregularly sampled, switch that series to a `ScatterLine`-family plottable in `CurvePlotBuilder`) |
| U07 | PiFM analysis UI | U01, V03, A15 | PiFM pages | Rewrite | P2 | – | planned |
| U08 | **Operation UI framework** (registry-driven launcher + parameter-editor strategy) — replaces U03's hardcoded launcher | U03, F04 | process dialogs | New (registry-driven) | P1 | ✅ | review (`IOperationLauncher` (Application) projects the operation registry → launcher items via `ApplicableTo(kind)`, grouped by category from `OutputKind` (Process/Measure); `GetForm` projects any operation's `ParameterSchema` → generic editor fields; `RunAsync` coerces UI value primitives back to CLR types, validates, and applies the output-kind policy (transform → derived active + Before/After; artifact → attached measurement). Shell launcher is data-bound to the registry; editor strategy = semantic override (Flatten hand-built, Statistics run-now) else the generic `ParameterFormViewModel`; **A03+ ops appear with no shell edits**; 8 headless tests; launcher + editors L render verified) |

## AI / ML / Docs
| ID | Task | Depends | Prio | MVP | Status |
|---|---|---|---|---|---|
| AI01 | Workflow engine (serialize/run/cache) | F04, F05 | P2 | – | planned |
| AI02 | `IAssistant` NL→workflow proposal + schema/registry validation | AI01 | P3 | – | planned |
| AI03 | Approval + provenance of AI steps | AI01, F05 | P3 | – | planned |
| ML01 | ML operation host (model versioning) | F04, F05 | P3 | – | planned |
| ML02 | First ML op (e.g. artifact detection) | ML01 | P3 | – | planned |
| DOC01 | Keep docs in sync (per completion, doc 41) | ongoing | P0 | ✅ | planned |

## MVP task set (P0)
F00, F01, F02, F03, D01, F04, F05, MV00, T01, FF01, W01, V00, V01, V02, A01, A02, T02, P01, **UX01,
UIX01, UIX02, UIX03,** U01, U02, DOC01. Grouped into 4 verification checkpoints — see
[`32-dependency-roadmap.md`](32-dependency-roadmap.md) §MVP checkpoints. This set == **EPIC-MVP01**
in [`35-product-epics-roadmap.md`](35-product-epics-roadmap.md).

## Product Epics
The full product beyond the Image MVP is organized as **vertical-slice Epics** (Image / Profile /
Spectroscopy / PiFM / AI) in [`35-product-epics-roadmap.md`](35-product-epics-roadmap.md), with a
Task↔Epic mapping. This backlog remains the authoritative per-task list and status source.

## Notes
- Once **F04** is stable, operations `A02…A16` are independent, parallelizable tasks.
- `A17` (native stitch), `AI*`, `ML*` require an ADR before starting.
- **MV00 is decoupled from the new domain** (it drives the legacy engine) and can run early, in
  parallel with the foundation tasks — golden data must exist before A01 parity (T02).
- Specs currently written: F00, F01, F03, F04, F05, D01, FF01, W01, MV00, UX01, V00, A01, P01
  (foundation + MVP boundary). Later-feature specs (A03–A16, FF03/04, P02) are written when the
  foundation + first vertical slice are stable (doc 41 §5).
