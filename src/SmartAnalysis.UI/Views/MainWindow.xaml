<Window x:Class="SmartAnalysis.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ds="clr-namespace:SmartAnalysis.UI.DesignSystem.Controls"
        xmlns:ctl="clr-namespace:SmartAnalysis.UI.Controls"
        xmlns:conv="clr-namespace:SmartAnalysis.UI.Converters"
        xmlns:vm="clr-namespace:SmartAnalysis.UI.ViewModels"
        Title="SmartAnalysis" Height="740" Width="1220" MinHeight="560" MinWidth="1000"
        Background="{DynamicResource SA.Brush.Background.App}"
        TextElement.Foreground="{DynamicResource SA.Brush.Text.Primary}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="Bool2Vis"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="NotBool2Vis"/>
        <conv:BooleanToOpacityConverter x:Key="Bool2Dim"/>
        <conv:ResourceKeyToGeometryConverter x:Key="Key2Geo"/>
        <conv:RatioToHeightConverter x:Key="Ratio2H"/>
        <conv:BooleanToGridLengthConverter x:Key="Bool2Row"/>

        <!-- Explorer lineage node -->
        <HierarchicalDataTemplate DataType="{x:Type vm:DatasetNodeViewModel}" ItemsSource="{Binding Children}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="2" Background="{DynamicResource SA.Brush.Accent.Primary}"
                        Margin="{DynamicResource SA.Gap.Right2}"
                        Visibility="{Binding IsActive, Converter={StaticResource Bool2Vis}}"/>
                <ds:IconPresenter Grid.Column="1" Data="{Binding IconKey, Converter={StaticResource Key2Geo}}"
                                  Margin="{DynamicResource SA.Gap.Right2}"/>
                <TextBlock Grid.Column="2" Text="{Binding Label}" VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis" Style="{StaticResource SA.Text.Body}"/>
                <Border Grid.Column="3" VerticalAlignment="Center" Margin="{DynamicResource SA.Gap.Right2}"
                        Background="{DynamicResource SA.Brush.Surface.Hover}"
                        BorderBrush="{DynamicResource SA.Brush.Border.Default}"
                        BorderThickness="{DynamicResource SA.Stroke.Hairline}"
                        CornerRadius="{DynamicResource SA.Radius.Sm}" Padding="{DynamicResource SA.Gap.1}"
                        Visibility="{Binding IsInComparison, Converter={StaticResource Bool2Vis}}">
                    <TextBlock Text="vs" Style="{StaticResource SA.Text.Micro}"
                               Foreground="{DynamicResource SA.Brush.Accent.OnSurface}"/>
                </Border>
                <ds:IconPresenter Grid.Column="4" Data="{StaticResource SA.Icon.Dot}"
                                  Foreground="{DynamicResource SA.Brush.Accent.OnSurface}"
                                  Visibility="{Binding IsActive, Converter={StaticResource Bool2Vis}}"/>
            </Grid>
        </HierarchicalDataTemplate>

        <!-- Provenance row -->
        <DataTemplate DataType="{x:Type vm:HistoryRowViewModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/><ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ds:IconPresenter Grid.Column="0" Width="13" Height="13" Data="{DynamicResource SA.Icon.Check}"
                                  Foreground="{DynamicResource SA.Brush.Status.Success}"/>
                <TextBlock Grid.Column="1" Text="{Binding Order}" Style="{StaticResource SA.Text.Micro}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding Operation}" VerticalAlignment="Center"
                           Margin="{DynamicResource SA.Gap.Right2}" Style="{StaticResource SA.Text.Caption}"/>
                <TextBlock Grid.Column="3" Text="{Binding Summary}" VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis" Style="{StaticResource SA.Text.Micro}"/>
            </Grid>
        </DataTemplate>

        <!-- Statistics readout row -->
        <DataTemplate DataType="{x:Type vm:ReadoutViewModel}">
            <Grid Margin="{DynamicResource SA.Gap.Bottom1}">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Name}" Style="{StaticResource SA.Text.Caption}"/>
                <TextBlock Grid.Column="1" Text="{Binding Value}" Style="{StaticResource SA.Text.Numeric}"/>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <!-- ============ Command bar (global only) ============ -->
        <Border Grid.Row="0" Style="{StaticResource SA.Toolbar}" Panel.ZIndex="2">
            <Grid Margin="{DynamicResource SA.Pad.Bar}">
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="18" Height="18" CornerRadius="{DynamicResource SA.Radius.Sm}"
                            Background="{DynamicResource SA.Brush.Accent.Primary}" Margin="{DynamicResource SA.Gap.Right2}"/>
                    <TextBlock Text="{Binding WorkspaceName}" VerticalAlignment="Center" Style="{StaticResource SA.Text.Subtitle}"/>
                    <Ellipse Width="6" Height="6" Fill="{DynamicResource SA.Brush.Accent.Primary}"
                             Margin="{DynamicResource SA.Gap.Left2}" VerticalAlignment="Center"
                             Visibility="{Binding HasUnsavedChanges, Converter={StaticResource Bool2Vis}}"/>
                    <Border Width="1" Height="20" Background="{DynamicResource SA.Brush.Border.Default}" Margin="{DynamicResource SA.Gap.3}"/>
                    <Button Command="{Binding OpenWorkspaceCommand}" Style="{StaticResource SA.Button.Secondary}" Margin="{DynamicResource SA.Gap.Right2}"
                            ToolTip="Open a saved workspace folder">
                        <StackPanel Orientation="Horizontal">
                            <ds:IconPresenter Data="{StaticResource SA.Icon.FolderOpen}" Margin="{DynamicResource SA.Gap.Right2}"
                                              Foreground="{DynamicResource SA.Brush.Text.Primary}"/>
                            <TextBlock Text="Open" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ImportCommand}" Style="{StaticResource SA.Button.Secondary}" Margin="{DynamicResource SA.Gap.Right2}"
                            ToolTip="Import a scan into the workspace">
                        <StackPanel Orientation="Horizontal">
                            <ds:IconPresenter Data="{StaticResource SA.Icon.Import}" Margin="{DynamicResource SA.Gap.Right2}"
                                              Foreground="{DynamicResource SA.Brush.Text.Primary}"/>
                            <TextBlock Text="Import" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding SaveCommand}" Style="{StaticResource SA.Button.Secondary}"
                            ToolTip="Save the workspace to a folder">
                        <StackPanel Orientation="Horizontal">
                            <ds:IconPresenter Data="{StaticResource SA.Icon.Save}" Margin="{DynamicResource SA.Gap.Right2}"
                                              Foreground="{DynamicResource SA.Brush.Text.Primary}"/>
                            <TextBlock Text="Save" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Border Width="1" Height="20" Background="{DynamicResource SA.Brush.Border.Default}" Margin="{DynamicResource SA.Gap.3}"/>
                    <Button x:Name="AnalyzeBtn" Command="{Binding ToggleLauncherCommand}"
                            Style="{StaticResource SA.Button.Primary}" Margin="{DynamicResource SA.Gap.Right2}">
                        <StackPanel Orientation="Horizontal">
                            <ds:IconPresenter Data="{StaticResource SA.Icon.Parameters}" Margin="{DynamicResource SA.Gap.Right2}"
                                              Foreground="{DynamicResource SA.Brush.Text.OnAccent}"/>
                            <TextBlock Text="Analyze" VerticalAlignment="Center"/>
                            <ds:IconPresenter Data="{StaticResource SA.Icon.ChevronDown}" Width="14" Height="14"
                                              Margin="{DynamicResource SA.Gap.Left2}" Foreground="{DynamicResource SA.Brush.Text.OnAccent}"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource SA.Button.Toolbar}" IsEnabled="False" ToolTip="Multi-dataset compare (later).">
                        <StackPanel Orientation="Horizontal">
                            <ds:IconPresenter Data="{StaticResource SA.Icon.Compare}" Margin="{DynamicResource SA.Gap.Right2}"/>
                            <TextBlock Text="Compare" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Command="{Binding ToggleThemeCommand}" Style="{StaticResource SA.Button.Toolbar}" Margin="{DynamicResource SA.Gap.Right2}">
                        <StackPanel Orientation="Horizontal">
                            <ds:IconPresenter Data="{StaticResource SA.Icon.Theme}" Margin="{DynamicResource SA.Gap.Right2}"/>
                            <TextBlock Text="{Binding ThemeToggleLabel}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <ds:IconPresenter Data="{StaticResource SA.Icon.Assistant}" Foreground="{DynamicResource SA.Brush.Accent.OnSurface}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Operation launcher popover -->
        <Popup x:Name="LauncherPopup" IsOpen="{Binding IsLauncherOpen}" PlacementTarget="{Binding ElementName=AnalyzeBtn}"
               Placement="Bottom" StaysOpen="False" AllowsTransparency="True" VerticalOffset="4">
            <Border Width="300" Background="{DynamicResource SA.Brush.Surface.Raised}"
                    BorderBrush="{DynamicResource SA.Brush.Border.Default}" BorderThickness="{DynamicResource SA.Stroke.Hairline}"
                    CornerRadius="{DynamicResource SA.Radius.Md}">
                <Border.Resources>
                    <!-- Registry-driven items, grouped by category (Process / Measure / …). -->
                    <CollectionViewSource x:Key="LauncherView" Source="{Binding LauncherItems}">
                        <CollectionViewSource.GroupDescriptions>
                            <PropertyGroupDescription PropertyName="CategoryLabel"/>
                        </CollectionViewSource.GroupDescriptions>
                    </CollectionViewSource>
                </Border.Resources>
                <ItemsControl ItemsSource="{Binding Source={StaticResource LauncherView}}" Margin="{DynamicResource SA.Gap.2}">
                    <ItemsControl.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <!-- Indented (SA.Pad.LauncherGroup) so the group label sits above the item labels. -->
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Pad.LauncherGroup}"/>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ItemsControl.GroupStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:OperationLauncherItemViewModel}">
                            <Button Command="{Binding LaunchCommand}" Style="{StaticResource SA.Button.Toolbar}"
                                    HorizontalContentAlignment="Left" HorizontalAlignment="Stretch"
                                    ToolTip="{Binding Summary}">
                                <StackPanel Orientation="Horizontal">
                                    <ds:IconPresenter Margin="{DynamicResource SA.Gap.Right2}"
                                                      Data="{Binding IconKey, Converter={StaticResource Key2Geo}}"/>
                                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Style="{StaticResource SA.Text.Body}"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </Popup>

        <!-- ============ Body: Navigator · Stage · Inspector ============ -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="234" MinWidth="180"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="304" MinWidth="260"/>
            </Grid.ColumnDefinitions>

            <!-- Navigator (recessed rail) + provenance strip -->
            <Border Grid.Column="0" Background="{DynamicResource SA.Brush.Background.Sidebar}"
                    BorderBrush="{DynamicResource SA.Brush.Border.Default}" BorderThickness="{DynamicResource SA.Stroke.Hairline.Right}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="WORKSPACE" Margin="{DynamicResource SA.Pad.Section}"
                               Style="{StaticResource SA.Text.SectionLabel}"/>
                    <Border DockPanel.Dock="Bottom" Background="{DynamicResource SA.Brush.Background.Sidebar}"
                            BorderBrush="{DynamicResource SA.Brush.Border.Subtle}" BorderThickness="{DynamicResource SA.Stroke.Hairline.Top}">
                        <DockPanel Margin="{DynamicResource SA.Gap.2}">
                            <TextBlock DockPanel.Dock="Top" Text="PROVENANCE" Margin="{DynamicResource SA.Gap.2}"
                                       Style="{StaticResource SA.Text.SectionLabel}"/>
                            <TextBlock DockPanel.Dock="Bottom" Text="Select a step to inspect it (read-only)."
                                       Margin="{DynamicResource SA.Gap.2}" Style="{StaticResource SA.Text.Micro}"
                                       Visibility="{Binding HasActive, Converter={StaticResource Bool2Vis}}"/>
                            <ListBox x:Name="ProvenanceList" ItemsSource="{Binding HistoryRows}" MaxHeight="120"
                                     Background="Transparent" BorderThickness="0"
                                     SelectionChanged="ProvenanceList_SelectionChanged"/>
                        </DockPanel>
                    </Border>
                    <TreeView x:Name="ExplorerTree" ItemsSource="{Binding ExplorerNodes}"
                              SelectedItemChanged="ExplorerTree_SelectedItemChanged"
                              Background="Transparent" BorderThickness="0" Padding="{DynamicResource SA.Gap.1}">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem" BasedOn="{StaticResource SA.TreeViewItem}">
                                <Setter Property="IsExpanded" Value="True"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>
                </DockPanel>
            </Border>

            <!-- STAGE (primary, brightest, soft elevation) -->
            <Border Grid.Column="1" Background="{DynamicResource SA.Brush.Surface.Default}" Panel.ZIndex="1">
                <Border.Effect><DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.10"/></Border.Effect>
                <DockPanel>
                    <!-- viewer toolbar (single mode) -->
                    <Border DockPanel.Dock="Top" BorderBrush="{DynamicResource SA.Brush.Border.Subtle}" BorderThickness="{DynamicResource SA.Stroke.Hairline.Bottom}"
                            Visibility="{Binding IsSingleImage, Converter={StaticResource Bool2Vis}}">
                        <Grid Height="42" Margin="{DynamicResource SA.Pad.Bar}">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <Border Grid.Column="0" ClipToBounds="True">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding ActiveTitle}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Style="{StaticResource SA.Text.BodyStrong}"/>
                                    <TextBlock Text="{Binding ActiveSubtitle}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                                               Margin="{DynamicResource SA.Gap.Left2}" Style="{StaticResource SA.Text.Secondary}"/>
                                </StackPanel>
                            </Border>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <Button Click="Fit_Click" Style="{StaticResource SA.Button.Toolbar}"
                                        Visibility="{Binding Is3D, Converter={StaticResource NotBool2Vis}}">
                                    <StackPanel Orientation="Horizontal">
                                        <ds:IconPresenter Data="{StaticResource SA.Icon.ZoomFit}" Margin="{DynamicResource SA.Gap.Right2}"/>
                                        <TextBlock Text="Fit" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <CheckBox Style="{StaticResource SA.CheckBox}" Content="3D" VerticalAlignment="Center"
                                          Margin="{DynamicResource SA.Gap.Left2}" IsChecked="{Binding Is3D}"
                                          Visibility="{Binding CanToggle3D, Converter={StaticResource Bool2Vis}}"
                                          ToolTip="View the active image as a 3D surface"/>
                                <CheckBox Style="{StaticResource SA.CheckBox}" Content="ROI" VerticalAlignment="Center"
                                          Margin="{DynamicResource SA.Gap.Left2}" IsChecked="{Binding RoiEnabled}"
                                          Visibility="{Binding CanUseRoi, Converter={StaticResource Bool2Vis}}"
                                          ToolTip="Draw a region of interest; region ops (Roughness) apply to it"/>
                                <CheckBox Style="{StaticResource SA.CheckBox}" Content="Ellipse" VerticalAlignment="Center"
                                          Margin="{DynamicResource SA.Gap.Left2}" IsChecked="{Binding RoiIsEllipse}"
                                          Visibility="{Binding RoiEnabled, Converter={StaticResource Bool2Vis}}"
                                          ToolTip="Elliptical ROI (else rectangular)"/>
                                <!-- Palette: colormap picker + Auto/Manual value range (legacy-parity). Kept compact
                                     so it doesn't crowd out Export at the min window width. -->
                                <ComboBox Style="{StaticResource SA.ComboBox}" Width="94" VerticalAlignment="Center"
                                          Margin="{DynamicResource SA.Gap.Left2}" ToolTip="Colormap"
                                          ItemsSource="{Binding AvailableColormaps}" SelectedItem="{Binding ColormapName}"/>
                                <CheckBox Style="{StaticResource SA.CheckBox}" Content="Auto" VerticalAlignment="Center"
                                          Margin="{DynamicResource SA.Gap.Left2}" IsChecked="{Binding AutoRange}"
                                          ToolTip="Auto range (each image's data min/max) vs. a manual range"/>
                                <TextBox Style="{StaticResource SA.TextBox}" Width="50" VerticalAlignment="Center"
                                         Margin="{DynamicResource SA.Gap.Left2}" ToolTip="Palette minimum"
                                         IsEnabled="{Binding ManualRangeEnabled}"
                                         Text="{Binding RangeMin, StringFormat=G4}"/>
                                <TextBox Style="{StaticResource SA.TextBox}" Width="50" VerticalAlignment="Center"
                                         Margin="{DynamicResource SA.Gap.Left1}" ToolTip="Palette maximum"
                                         IsEnabled="{Binding ManualRangeEnabled}"
                                         Text="{Binding RangeMax, StringFormat=G4}"/>
                                <Border Width="1" Height="18" Background="{DynamicResource SA.Brush.Border.Default}" Margin="{DynamicResource SA.Gap.2}"/>
                                <Button Click="Export_Click" Style="{StaticResource SA.Button.Toolbar}">
                                    <StackPanel Orientation="Horizontal">
                                        <ds:IconPresenter Data="{StaticResource SA.Icon.Import}" Margin="{DynamicResource SA.Gap.Right2}"/>
                                        <TextBlock Text="Export" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <!-- compare toolbar (comparison mode) -->
                    <Border DockPanel.Dock="Top" BorderBrush="{DynamicResource SA.Brush.Border.Subtle}" BorderThickness="{DynamicResource SA.Stroke.Hairline.Bottom}"
                            Visibility="{Binding IsBeforeAfter, Converter={StaticResource Bool2Vis}}">
                        <Grid Height="42" Margin="{DynamicResource SA.Pad.Bar}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ListBox Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource SA.Segmented}" ItemContainerStyle="{StaticResource SA.Segmented.Item}" SelectedIndex="0">
                                <ListBoxItem>Split</ListBoxItem><ListBoxItem IsEnabled="False">Overlay</ListBoxItem>
                                <ListBoxItem IsEnabled="False">Difference</ListBoxItem>
                            </ListBox>
                            <!-- Informational, lowest priority: clipped to its column so it never overlaps the
                                 mode switch / exit button as the window narrows. -->
                            <Border Grid.Column="1" ClipToBounds="True" Margin="{DynamicResource SA.Gap.2}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <ds:IconPresenter Data="{StaticResource SA.Icon.Cursor}" Foreground="{DynamicResource SA.Brush.Accent.OnSurface}" Margin="{DynamicResource SA.Gap.Right2}"/>
                                    <TextBlock Text="Synced nav" VerticalAlignment="Center" Style="{StaticResource SA.Text.Caption}"/>
                                    <Border Width="1" Height="18" Background="{DynamicResource SA.Brush.Border.Default}" Margin="{DynamicResource SA.Gap.3}"/>
                                    <TextBlock VerticalAlignment="Center" Style="{StaticResource SA.Text.Caption}">
                                        <Run Text="Same X/Y" FontWeight="SemiBold"/><Run Text=" · Independent Z ranges"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                            <Button Grid.Column="2" VerticalAlignment="Center" Command="{Binding ExitCompareCommand}" Style="{StaticResource SA.Button.Secondary}">
                                <TextBlock Text="Exit compare"/>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- content: the viewer stack, plus a docked live profile chart (line-profile mode) -->
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                        <ctl:AfmImageView x:Name="SingleImage" Margin="{DynamicResource SA.Gap.2}" IsRangeEditable="True"
                                          Visibility="{Binding ShowSourceImagePane, Converter={StaticResource Bool2Vis}}"/>
                        <ctl:AfmSurfaceView x:Name="SingleSurface" Margin="{DynamicResource SA.Gap.2}"
                                            Visibility="{Binding ShowSingle3D, Converter={StaticResource Bool2Vis}}"/>
                        <ctl:AfmCurveView x:Name="SingleCurve" Margin="{DynamicResource SA.Gap.2}"
                                          Visibility="{Binding IsSingleCurve, Converter={StaticResource Bool2Vis}}"/>
                        <!-- Spectroscopy stage (doc 26 §22.1): a map is many curves measured at PLACES, so the
                             Stage is the surface with its points on it. The curve is what you get by choosing
                             one, and lives in the Inspector (§22.2) until it is extracted as a dataset. -->
                        <!-- A Grid, not a DockPanel: the two stage views are alternatives occupying the same row,
                             and a DockPanel would dock the first to Left instead of letting it fill. -->
                        <Grid x:Name="SpectroscopyPanel" Margin="{DynamicResource SA.Gap.2}"
                              Visibility="{Binding IsSpectroscopy, Converter={StaticResource Bool2Vis}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="{Binding ShowCurveBelowStage, Converter={StaticResource Bool2Row}, ConverterParameter=0.55}"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="{DynamicResource SA.Brush.Surface.Raised}"
                                    Visibility="{Binding ShowSpectroscopyToolbar, Converter={StaticResource Bool2Vis}}"
                                    Padding="{DynamicResource SA.Gap.2}" Margin="{DynamicResource SA.Gap.Bottom1}">
                                <DockPanel>
                                    <Button x:Name="MapPointBack" DockPanel.Dock="Left" Content="&#x25C0;" Width="28"
                                            Visibility="{Binding IsForceVolume, Converter={StaticResource Bool2Vis}}"
                                            IsEnabled="{Binding CanStepMapPointBack}" Click="MapPointBack_Click"/>
                                    <Button x:Name="MapPointForward" DockPanel.Dock="Left" Content="&#x25B6;" Width="28"
                                            Margin="{DynamicResource SA.Gap.Left1}"
                                            Visibility="{Binding IsForceVolume, Converter={StaticResource Bool2Vis}}"
                                            IsEnabled="{Binding CanStepMapPointForward}" Click="MapPointForward_Click"/>
                                    <Button x:Name="ShowSurface" DockPanel.Dock="Right" Content="Surface" MinWidth="64"
                                            Visibility="{Binding CanShowVolume, Converter={StaticResource Bool2Vis}}"
                                            Command="{Binding ShowSurfaceCommand}"/>
                                    <Button x:Name="ShowVolume" DockPanel.Dock="Right" Content="Volume" MinWidth="64"
                                            Margin="{DynamicResource SA.Gap.Left1}"
                                            Visibility="{Binding CanShowVolume, Converter={StaticResource Bool2Vis}}"
                                            Command="{Binding ShowVolumeCommand}"/>
                                    <TextBlock Text="{Binding SpectroscopyLabel}" Margin="{DynamicResource SA.Gap.Left2}"
                                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                </DockPanel>
                            </Border>

                            <!-- The surface, with a marker at every place a curve was measured. Shown only when
                                 the file carried one; otherwise the curve takes the Stage, because a map with no
                                 surface has nothing spatial to show. -->
                            <ctl:AfmImageView Grid.Row="1" x:Name="SpectroscopySurface"
                                              Visibility="{Binding ShowSpectroscopyImage, Converter={StaticResource Bool2Vis}}"/>
                            <ctl:AfmCurveView Grid.Row="1" x:Name="SpectroscopyCurve"
                                              Visibility="{Binding ShowCurveOnStage, Converter={StaticResource Bool2Vis}}"/>

                            <!-- doc 26 SS22.6: the selected point's curve belongs to the POINT, not to a role, so
                                 it survives every view switch. It sits under the picture and across the Stage
                                 because a force curve is wide: in the Inspector rail it had 271px to say
                                 everything in, and making it taller there only made it a taller sliver. -->
                            <Grid Grid.Row="2" Margin="{DynamicResource SA.Gap.Top2}"
                                  Visibility="{Binding ShowCurveBelowStage, Converter={StaticResource Bool2Vis}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="CURVE" Style="{StaticResource SA.Text.SectionLabel}"
                                           Margin="{DynamicResource SA.Gap.Bottom1}"/>
                                <ctl:AfmCurveView Grid.Row="1" x:Name="PointCurve" MinHeight="180"/>
                            </Grid>

                            <!-- Until the first picture arrives there is nothing on the stage. Saying so is the
                                 difference between "working" and "nothing happened". -->
                            <TextBlock Grid.Row="1" Text="Measuring the map…"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Style="{StaticResource SA.Text.Secondary}"
                                       Visibility="{Binding IsVolumeComputing, Converter={StaticResource Bool2Vis}}"/>

                            <!-- A setting the map cannot be measured with leaves no picture. Saying so beats an
                                 empty stage, and both beat the previous settings' picture left standing. -->
                            <TextBlock Grid.Row="1" Text="{Binding VolumeUnavailable}" TextWrapping="Wrap"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       MaxWidth="360" TextAlignment="Center"
                                       Style="{StaticResource SA.Text.Secondary}"
                                       Visibility="{Binding HasVolumeUnavailable, Converter={StaticResource Bool2Vis}}"/>
                        </Grid>
                        <!-- Two equal panes; each frames its image exactly like the single stage (same Gap.2 margin
                             on the AfmImageView) so the before/after images and their fixed-width palettes line up. -->
                        <Grid x:Name="CompareContent" Visibility="{Binding ShowComparePanes, Converter={StaticResource Bool2Vis}}">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="1"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <DockPanel Grid.Column="0">
                                <TextBlock DockPanel.Dock="Top" Text="{Binding CompareBeforeLabel}" Margin="{DynamicResource SA.Pad.Bar}"
                                           Foreground="{DynamicResource SA.Brush.Text.Tertiary}" Style="{StaticResource SA.Text.SectionLabel}"/>
                                <ctl:AfmImageView x:Name="BeforeImageView" Margin="{DynamicResource SA.Gap.2}"/>
                            </DockPanel>
                            <Border Grid.Column="1" Background="{DynamicResource SA.Brush.Border.Default}"/>
                            <DockPanel Grid.Column="2">
                                <TextBlock DockPanel.Dock="Top" Text="{Binding CompareAfterLabel}" Margin="{DynamicResource SA.Pad.Bar}"
                                           Foreground="{DynamicResource SA.Brush.Accent.OnSurface}" Style="{StaticResource SA.Text.SectionLabel}"/>
                                <ctl:AfmImageView x:Name="AfterImageView" Margin="{DynamicResource SA.Gap.2}"/>
                            </DockPanel>
                        </Grid>

                        <!-- empty / first-run -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Visibility="{Binding IsEmpty, Converter={StaticResource Bool2Vis}}">
                            <ds:IconPresenter Data="{StaticResource SA.Icon.FolderOpen}" Width="40" Height="40"
                                              Foreground="{DynamicResource SA.Brush.Text.Tertiary}"
                                              HorizontalAlignment="Center" Margin="{DynamicResource SA.Gap.Bottom2}"/>
                            <TextBlock Text="No workspace open" HorizontalAlignment="Center" Style="{StaticResource SA.Text.Subtitle}"/>
                            <TextBlock Text="Open a scan, or start with the bundled sample." HorizontalAlignment="Center"
                                       Margin="{DynamicResource SA.Gap.Bottom2}" Style="{StaticResource SA.Text.Secondary}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Command="{Binding ImportCommand}" Content="Import scan…" Style="{StaticResource SA.Button.Primary}" Margin="{DynamicResource SA.Gap.Right2}"/>
                                <Button Command="{Binding OpenSampleCommand}" Content="Open sample" Style="{StaticResource SA.Button.Secondary}"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- status banner -->
                        <Border VerticalAlignment="Bottom" Style="{StaticResource SA.Banner.Error}" Margin="{DynamicResource SA.Gap.3}"
                                Visibility="{Binding HasStatus, Converter={StaticResource Bool2Vis}}">
                            <StackPanel Orientation="Horizontal">
                                <ds:IconPresenter Data="{StaticResource SA.Icon.Error}" Margin="{DynamicResource SA.Gap.Right2}"
                                                  Foreground="{DynamicResource SA.Brush.Banner.Error.Foreground}"/>
                                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"
                                           Foreground="{DynamicResource SA.Brush.Banner.Error.Foreground}" Style="{StaticResource SA.Text.Body}"/>
                            </StackPanel>
                        </Border>
                        </Grid>

                        <!-- Live profile chart: shown while a line-profile form is open; updates as the line drags -->
                        <Border x:Name="LineProfilePanel" Grid.Row="1" Height="220" Visibility="Collapsed"
                                BorderBrush="{DynamicResource SA.Brush.Border.Default}" BorderThickness="{DynamicResource SA.Stroke.Hairline.Top}"
                                Background="{DynamicResource SA.Brush.Surface.Default}">
                            <DockPanel Margin="{DynamicResource SA.Gap.2}">
                                <TextBlock DockPanel.Dock="Top" Text="LINE PROFILE" Margin="{DynamicResource SA.Gap.2}"
                                           Foreground="{DynamicResource SA.Brush.Text.Tertiary}" Style="{StaticResource SA.Text.SectionLabel}"/>
                                <ctl:AfmCurveView x:Name="ProfileChart"/>
                            </DockPanel>
                        </Border>
                    </Grid>
                </DockPanel>
            </Border>

            <!-- INSPECTOR (recessed rail, role-switching) -->
            <Border Grid.Column="2" Background="{DynamicResource SA.Brush.Background.Sidebar}"
                    BorderBrush="{DynamicResource SA.Brush.Border.Default}" BorderThickness="{DynamicResource SA.Stroke.Hairline.Left}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- The roles overlay each other; only one is visible at a time. -->
                    <Grid Grid.Row="0">
                    <!-- placeholder when no image -->
                    <StackPanel Margin="{DynamicResource SA.Pad.Panel}" VerticalAlignment="Center" HorizontalAlignment="Center"
                                Visibility="{Binding HasNothingToInspect, Converter={StaticResource Bool2Vis}}">
                        <ds:IconPresenter Data="{StaticResource SA.Icon.Parameters}" Width="20" Height="20"
                                          HorizontalAlignment="Center" Margin="{DynamicResource SA.Gap.Bottom2}"/>
                        <TextBlock Text="Select an image" HorizontalAlignment="Center" Style="{StaticResource SA.Text.Secondary}"/>
                    </StackPanel>

                    <!-- Dataset properties -->
                    <StackPanel Margin="{DynamicResource SA.Pad.Panel}"
                                Visibility="{Binding RoleIsDataset, Converter={StaticResource Bool2Vis}}">
                        <StackPanel Visibility="{Binding HasActiveImage, Converter={StaticResource Bool2Vis}}">
                            <TextBlock Text="DATASET" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Bottom2}"/>
                            <TextBlock Text="{Binding ActiveTitle}" Style="{StaticResource SA.Text.Title}"/>
                            <TextBlock Text="{Binding ActiveSubtitle}" Margin="{DynamicResource SA.Gap.Bottom2}" Style="{StaticResource SA.Text.Secondary}"/>
                            <TextBlock Text="ACQUISITION" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Top2}"/>
                            <Grid Margin="{DynamicResource SA.Gap.Bottom1}">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Instrument" Style="{StaticResource SA.Text.Caption}"/>
                                <TextBlock Grid.Column="1" Text="{Binding ActiveMeta}" Style="{StaticResource SA.Text.Numeric}"/>
                            </Grid>
                            <!-- Simple measurements, read directly here (no Analyze needed) -->
                            <StackPanel Visibility="{Binding HasLiveMeasurements, Converter={StaticResource Bool2Vis}}"
                                        DataContext="{Binding LiveMeasurements}">
                                <TextBlock Text="MEASUREMENTS" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Top2}"/>
                                <ItemsControl ItemsSource="{Binding Readouts}" Margin="{DynamicResource SA.Gap.Top1}"/>
                                <StackPanel Visibility="{Binding HasHistogram, Converter={StaticResource Bool2Vis}}">
                                    <ItemsControl ItemsSource="{Binding HistogramBars}" Height="52" Margin="{DynamicResource SA.Gap.Top2}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate><StackPanel Orientation="Horizontal" VerticalAlignment="Bottom"/></ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="7" Margin="{DynamicResource SA.Gap.BarX}" VerticalAlignment="Bottom"
                                                        Background="{DynamicResource SA.Brush.Accent.Primary}"
                                                        Height="{Binding Converter={StaticResource Ratio2H}, ConverterParameter=48}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="Height histogram · golden-verified (MV00)" Style="{StaticResource SA.Text.Micro}" Margin="{DynamicResource SA.Gap.Top2}"/>
                                </StackPanel>
                            </StackPanel>
                            <Border Style="{StaticResource SA.Card}" Background="{DynamicResource SA.Brush.Surface.Sunken}"
                                    Margin="{DynamicResource SA.Gap.Top2}">
                                <TextBlock Text="Process · derive · view from Analyze ▾. Simple measurements show above."
                                           TextWrapping="Wrap" Style="{StaticResource SA.Text.Caption}"/>
                            </Border>
                        </StackPanel>
                    </StackPanel>

                    <!-- Profile props (doc 26 §5). A profile is a slice of an image; the Inspector says how far
                         it runs and where it was taken, the same two questions the map answers for a point. -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  Visibility="{Binding RoleIsDataset, Converter={StaticResource Bool2Vis}}">
                        <StackPanel Margin="{DynamicResource SA.Pad.Panel}"
                                    Visibility="{Binding IsProfile, Converter={StaticResource Bool2Vis}}">
                            <TextBlock Text="DATASET" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Bottom2}"/>
                            <TextBlock Text="{Binding ActiveTitle}" Style="{StaticResource SA.Text.Title}"/>
                            <TextBlock Text="{Binding ActiveSubtitle}" Style="{StaticResource SA.Text.Secondary}"/>

                            <TextBlock Text="PROFILE" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Top2}"/>
                            <TextBlock Text="{Binding ProfileSummary}" Style="{StaticResource SA.Text.Numeric}"/>
                            <TextBlock Text="{Binding ProfileSource}" Style="{StaticResource SA.Text.Caption}"
                                       TextWrapping="Wrap" Margin="{DynamicResource SA.Gap.Top1}"/>

                            <Border Style="{StaticResource SA.Card}" Background="{DynamicResource SA.Brush.Surface.Sunken}"
                                    Margin="{DynamicResource SA.Gap.Top2}">
                                <TextBlock Text="Fit · smooth · measure from Analyze ▾."
                                           TextWrapping="Wrap" Style="{StaticResource SA.Text.Caption}"/>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Map props / Curve props (doc 26 §22.2). The Inspector carries the selection: what the
                         point measured, on which channel pair, and the one step that turns it into a dataset. -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  Visibility="{Binding RoleIsDataset, Converter={StaticResource Bool2Vis}}">
                        <StackPanel Margin="{DynamicResource SA.Pad.Panel}"
                                    Visibility="{Binding IsSpectroscopy, Converter={StaticResource Bool2Vis}}">
                            <TextBlock Text="DATASET" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Bottom2}"/>
                            <TextBlock Text="{Binding ActiveTitle}" Style="{StaticResource SA.Text.Title}"/>
                            <TextBlock Text="{Binding ActiveSubtitle}" Style="{StaticResource SA.Text.Secondary}"/>

                            <StackPanel Visibility="{Binding IsForceVolume, Converter={StaticResource Bool2Vis}}">
                                <TextBlock Text="MAP" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Top2}"/>
                                <TextBlock Text="{Binding MapSummary}" Style="{StaticResource SA.Text.Numeric}"/>
                                <TextBlock Text="{Binding MapPointLabel}" Style="{StaticResource SA.Text.Caption}"
                                           TextWrapping="Wrap" Margin="{DynamicResource SA.Gap.Top1}"/>
                            </StackPanel>

                            <!-- The pair is a property of what you are looking at, so it lives here, not on the
                                 viewer toolbar (§9). Hidden when the file kept no channels to choose between. -->
                            <StackPanel Visibility="{Binding CanPickChannels, Converter={StaticResource Bool2Vis}}">
                                <TextBlock Text="CHANNEL PAIR" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Top2}"/>
                                <TextBlock Text="X" Style="{StaticResource SA.Text.Caption}"/>
                                <ComboBox ItemsSource="{Binding ChannelChoices}" DisplayMemberPath="Label"
                                          SelectedItem="{Binding SelectedXChannelItem}"/>
                                <TextBlock Text="Y" Style="{StaticResource SA.Text.Caption}" Margin="{DynamicResource SA.Gap.Top1}"/>
                                <ComboBox ItemsSource="{Binding ChannelChoices}" DisplayMemberPath="Label"
                                          SelectedItem="{Binding SelectedYChannelItem}"/>
                            </StackPanel>

                            <Button Content="Extract this point" Style="{StaticResource SA.Button.Primary}"
                                    Margin="{DynamicResource SA.Gap.Top2}" HorizontalAlignment="Stretch"
                                    Visibility="{Binding IsForceVolume, Converter={StaticResource Bool2Vis}}"
                                    Command="{Binding ExtractPointCommand}"/>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Operation config: a semantic editor (Flatten) or the generic schema form (U08) -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  Visibility="{Binding RoleIsOperation, Converter={StaticResource Bool2Vis}}">
                        <ContentControl Content="{Binding OperationEditor}" Margin="{DynamicResource SA.Pad.Panel}">
                            <ContentControl.Resources>
                                <!-- Semantic editor: Flatten's hand-built segmented/stepper controls -->
                                <DataTemplate DataType="{x:Type vm:FlattenPanelViewModel}">
                                    <StackPanel>
                                        <TextBlock Text="OPERATION" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Bottom2}"/>
                                        <TextBlock Text="Flatten" Style="{StaticResource SA.Text.Title}"/>
                                        <TextBlock Text="Subtract a per-line, averaged, or surface polynomial."
                                                   TextWrapping="Wrap" Margin="{DynamicResource SA.Gap.Bottom2}" Style="{StaticResource SA.Text.Secondary}"/>

                                        <TextBlock Text="Scope" Style="{StaticResource SA.Text.Caption}" Margin="{DynamicResource SA.Gap.Bottom1}"/>
                                        <ListBox Style="{StaticResource SA.Segmented}" ItemContainerStyle="{StaticResource SA.Segmented.Item}"
                                                 ItemsSource="{Binding ScopeOptions}" SelectedItem="{Binding Scope, Mode=TwoWay}"
                                                 Margin="{DynamicResource SA.Gap.Bottom2}"/>

                                        <TextBlock Text="Order (0–8)" Style="{StaticResource SA.Text.Caption}" Margin="{DynamicResource SA.Gap.Bottom1}"/>
                                        <Border BorderBrush="{DynamicResource SA.Brush.Border.Default}" BorderThickness="{DynamicResource SA.Stroke.Hairline}"
                                                CornerRadius="{DynamicResource SA.Radius.Sm}" HorizontalAlignment="Left" Margin="{DynamicResource SA.Gap.Bottom2}">
                                            <StackPanel Orientation="Horizontal">
                                                <Button Command="{Binding OrderDownCommand}" Style="{StaticResource SA.Button.Icon}">
                                                    <ds:IconPresenter Data="{StaticResource SA.Icon.ChevronDown}" Width="14" Height="14"/>
                                                </Button>
                                                <TextBlock Text="{Binding Order}" MinWidth="34" TextAlignment="Center" VerticalAlignment="Center"
                                                           Style="{StaticResource SA.Text.Numeric}"/>
                                                <Button Command="{Binding OrderUpCommand}" Style="{StaticResource SA.Button.Icon}">
                                                    <ds:IconPresenter Data="{StaticResource SA.Icon.ChevronRight}" Width="14" Height="14"/>
                                                </Button>
                                            </StackPanel>
                                        </Border>

                                        <TextBlock Text="Orientation" Style="{StaticResource SA.Text.Caption}" Margin="{DynamicResource SA.Gap.Bottom1}"/>
                                        <ListBox Style="{StaticResource SA.Segmented}" ItemContainerStyle="{StaticResource SA.Segmented.Item}"
                                                 ItemsSource="{Binding OrientationOptions}" SelectedItem="{Binding Orientation, Mode=TwoWay}"
                                                 IsEnabled="{Binding OrientationEnabled}" Margin="{DynamicResource SA.Gap.Bottom2}"/>

                                        <TextBlock Text="Basement" Style="{StaticResource SA.Text.Caption}" Margin="{DynamicResource SA.Gap.Bottom1}"/>
                                        <ComboBox ItemsSource="{Binding BasementOptions}" SelectedItem="{Binding Basement}" Margin="{DynamicResource SA.Gap.Bottom2}"/>

                                        <Border Style="{StaticResource SA.Banner.Error}" Margin="{DynamicResource SA.Gap.Bottom2}"
                                                Visibility="{Binding HasError, Converter={StaticResource Bool2Vis}}">
                                            <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Style="{StaticResource SA.Text.Caption}"/>
                                        </Border>
                                        <Border Style="{StaticResource SA.Banner.Warning}" Margin="{DynamicResource SA.Gap.Bottom2}"
                                                Visibility="{Binding HasWarnings, Converter={StaticResource Bool2Vis}}">
                                            <TextBlock Text="{Binding Warnings}" TextWrapping="Wrap" Style="{StaticResource SA.Text.Caption}"/>
                                        </Border>

                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="{DynamicResource SA.Gap.Top2}">
                                            <Button Content="{Binding ApplyLabel}" Command="{Binding ApplyCommand}" Style="{StaticResource SA.Button.Primary}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>

                                <!-- Generic editor: fields projected from any operation's schema (U08) -->
                                <DataTemplate DataType="{x:Type vm:ParameterFormViewModel}">
                                    <StackPanel>
                                        <TextBlock Text="OPERATION" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Bottom2}"/>
                                        <TextBlock Text="{Binding DisplayName}" Style="{StaticResource SA.Text.Title}"/>
                                        <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" Margin="{DynamicResource SA.Gap.Bottom2}" Style="{StaticResource SA.Text.Secondary}"/>

                                        <ItemsControl ItemsSource="{Binding Fields}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type vm:ParameterFieldViewModel}">
                                                    <!-- A field the current settings do not use stays put but inert:
                                                         a control that appears and vanishes as you work hides what
                                                         shapes the result instead of showing it. -->
                                                    <StackPanel Margin="{DynamicResource SA.Gap.Bottom2}"
                                                                IsEnabled="{Binding IsRelevant}"
                                                                Opacity="{Binding IsRelevant, Converter={StaticResource Bool2Dim}}">
                                                        <TextBlock Text="{Binding Label}" Style="{StaticResource SA.Text.Caption}" Margin="{DynamicResource SA.Gap.Bottom1}"/>
                                                        <ComboBox ItemsSource="{Binding Options}" DisplayMemberPath="Label" SelectedValuePath="Value"
                                                                  SelectedValue="{Binding Value, Mode=TwoWay}"
                                                                  Visibility="{Binding IsChoice, Converter={StaticResource Bool2Vis}}"/>
                                                        <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 Visibility="{Binding IsNumber, Converter={StaticResource Bool2Vis}}"/>
                                                        <CheckBox IsChecked="{Binding Value, Mode=TwoWay}"
                                                                  Visibility="{Binding IsBoolean, Converter={StaticResource Bool2Vis}}"/>
                                                        <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 Visibility="{Binding IsText, Converter={StaticResource Bool2Vis}}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <Border Style="{StaticResource SA.Banner.Error}" Margin="{DynamicResource SA.Gap.Bottom2}"
                                                Visibility="{Binding HasError, Converter={StaticResource Bool2Vis}}">
                                            <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Style="{StaticResource SA.Text.Caption}"/>
                                        </Border>
                                        <Border Style="{StaticResource SA.Banner.Warning}" Margin="{DynamicResource SA.Gap.Bottom2}"
                                                Visibility="{Binding HasWarnings, Converter={StaticResource Bool2Vis}}">
                                            <TextBlock Text="{Binding Warnings}" TextWrapping="Wrap" Style="{StaticResource SA.Text.Caption}"/>
                                        </Border>

                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="{DynamicResource SA.Gap.Top2}">
                                            <Button Content="{Binding ApplyLabel}" Command="{Binding ApplyCommand}" Style="{StaticResource SA.Button.Primary}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ContentControl.Resources>
                        </ContentControl>
                    </ScrollViewer>

                    <!-- Measurement result -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  Visibility="{Binding RoleIsResult, Converter={StaticResource Bool2Vis}}">
                        <StackPanel DataContext="{Binding Statistics}" Margin="{DynamicResource SA.Pad.Panel}">
                            <TextBlock Text="MEASUREMENT" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Bottom2}"/>
                            <Border Style="{StaticResource SA.Card}" Background="{DynamicResource SA.Brush.Surface.Sunken}" Margin="{DynamicResource SA.Gap.Bottom2}">
                                <TextBlock TextWrapping="Wrap" Style="{StaticResource SA.Text.Caption}">
                                    <Run Text="Attached to "/><Run Text="{Binding SourceLabel, Mode=OneWay}" FontWeight="SemiBold"/><Run Text=" — active unchanged"/>
                                </TextBlock>
                            </Border>
                            <ItemsControl ItemsSource="{Binding Readouts}"/>
                            <StackPanel Visibility="{Binding HasHistogram, Converter={StaticResource Bool2Vis}}">
                                <ItemsControl ItemsSource="{Binding HistogramBars}" Height="52" Margin="{DynamicResource SA.Gap.Top2}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate><StackPanel Orientation="Horizontal" VerticalAlignment="Bottom"/></ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="7" Margin="{DynamicResource SA.Gap.BarX}" VerticalAlignment="Bottom"
                                                    Background="{DynamicResource SA.Brush.Accent.Primary}"
                                                    Height="{Binding Converter={StaticResource Ratio2H}, ConverterParameter=48}"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="Height histogram · golden-verified (MV00)" Style="{StaticResource SA.Text.Micro}" Margin="{DynamicResource SA.Gap.Top2}"/>
                            </StackPanel>

                            <!-- Optional tabular result (e.g. the full peak list). Equal-width columns via UniformGrid. -->
                            <StackPanel Visibility="{Binding HasTable, Converter={StaticResource Bool2Vis}}" Margin="{DynamicResource SA.Gap.Top2}">
                                <ItemsControl ItemsSource="{Binding TableColumns}" Margin="{DynamicResource SA.Gap.Bottom1}">
                                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate><TextBlock Text="{Binding}" Style="{StaticResource SA.Text.Micro}"
                                                                 Foreground="{DynamicResource SA.Brush.Text.Tertiary}"/></DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <ItemsControl ItemsSource="{Binding TableRows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <ItemsControl ItemsSource="{Binding}" Margin="{DynamicResource SA.Gap.Bottom1}">
                                                <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate><TextBlock Text="{Binding}" Style="{StaticResource SA.Text.Numeric}"/></DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Provenance step inspector (read-only) -->
                    <StackPanel Margin="{DynamicResource SA.Pad.Panel}"
                                Visibility="{Binding RoleIsStep, Converter={StaticResource Bool2Vis}}">
                        <TextBlock Text="RECORDED STEP" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Bottom2}"/>
                        <TextBlock Text="{Binding SelectedStep.Operation}" Style="{StaticResource SA.Text.Title}"/>
                        <TextBlock Text="{Binding SelectedStep.OperationId}" Foreground="{DynamicResource SA.Brush.Text.Tertiary}"
                                   Margin="{DynamicResource SA.Gap.Bottom2}" Style="{StaticResource SA.Text.Micro}"/>

                        <!-- parameters that were applied -->
                        <TextBlock Text="PARAMETERS" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Bottom1}"
                                   Visibility="{Binding SelectedStep.HasParameters, Converter={StaticResource Bool2Vis}}"/>
                        <ItemsControl ItemsSource="{Binding SelectedStep.Parameters}"
                                      Visibility="{Binding SelectedStep.HasParameters, Converter={StaticResource Bool2Vis}}"
                                      Margin="{DynamicResource SA.Gap.Bottom2}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:StepParameterViewModel}">
                                    <Grid Margin="{DynamicResource SA.Gap.Bottom1}">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}" Style="{StaticResource SA.Text.Caption}"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Value}" Style="{StaticResource SA.Text.Numeric}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="No parameters." Style="{StaticResource SA.Text.Caption}" Margin="{DynamicResource SA.Gap.Bottom2}"
                                   Foreground="{DynamicResource SA.Brush.Text.Tertiary}"
                                   Visibility="{Binding SelectedStep.HasParameters, Converter={StaticResource NotBool2Vis}}"/>

                        <!-- warnings recorded when the step ran -->
                        <TextBlock Text="WARNINGS" Style="{StaticResource SA.Text.SectionLabel}" Margin="{DynamicResource SA.Gap.Bottom1}"
                                   Foreground="{DynamicResource SA.Brush.Status.Warning}"
                                   Visibility="{Binding SelectedStep.HasWarnings, Converter={StaticResource Bool2Vis}}"/>
                        <ItemsControl ItemsSource="{Binding SelectedStep.Warnings}" Margin="{DynamicResource SA.Gap.Bottom2}"
                                      Visibility="{Binding SelectedStep.HasWarnings, Converter={StaticResource Bool2Vis}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Style="{StaticResource SA.Text.Caption}"
                                               Foreground="{DynamicResource SA.Brush.Status.Warning}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Border Style="{StaticResource SA.Card}" Background="{DynamicResource SA.Brush.Surface.Sunken}">
                            <TextBlock Text="A recorded step is read-only. Selecting it does not change the active dataset."
                                       TextWrapping="Wrap" Style="{StaticResource SA.Text.Caption}"/>
                        </Border>
                    </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
