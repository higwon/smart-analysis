namespace SmartAnalysis.Visualization.Colormaps;

/// <summary>A 24-bit RGB color (data-colormap sample, not a UI-theme color). Immutable value type.</summary>
public readonly record struct Rgb(byte R, byte G, byte B);
