# SmartAnalysis.Analysis

**Headless** analysis operations + numeric algorithms (the operation contract + registry + the
operations themselves, in per-area folders: Image / Spectroscopy / Profile / Pifm).

- **Target:** `net8.0` (platform-neutral, runs with no UI). Warnings-as-errors.
- **References:** `Domain` only.
- **Must not** reference UI, WPF, charting, Infrastructure, or any commercial library.

> TASK-F00 creates this project empty. The operation contract arrives in F04; operations later.
